# 求职 Agent M6 主线质量闭环方案

日期：2026-05-11

## 1. 背景

M1 到 M5 已经完成了求职产品资产层、工具层、agent 契约、前端资产面板、执行进度面板和报告阅读体验的主要落地。当前阶段先暂停前端视觉继续优化，回到主线任务：验证真实求职链路是否稳定、可追溯、可持续迭代。

本阶段不追求大批量 smoke。真实模型调用会消耗 token，也会因为模型延迟和工具调用路径产生较长等待。M6 的策略是先用低批次、强断言、可复盘的方式跑通质量闭环。

## 2. 当前里程碑状态

### 2.1 已完成主线

- `CareerProductStore` 已作为求职产品资产事实源独立存在。
- `SessionArtifact` 仍是文件事实源，报告和生成简历等用户可见文件通过 artifact 承载。
- `AgentTask` 作为 multi-agent 委派生命周期事实源。
- `events` 作为运行审计日志。
- `memory` 不承担产品资产职责。
- 简历画像、职业画像、JD 分析、匹配报告、简历版本五类产品记录已接入工具层。
- agent 权限已经拆分，避免所有 agent 都能写所有 career 资产。
- 前端已经能展示求职资产、预览 artifact、显示执行进度、渲染结构化报告。

### 2.2 暂停项

以下前端体验优化先暂停，避免偏离主线：

- 报告正文继续做更细的视觉 polish。
- 行动建议区改成任务清单式交互。
- 资产面板更多动效和更细的阅读状态。
- 发行版去除调试入口，例如复制内部 ID。

这些不是删除，只是后置。

## 3. M6 目标

M6 的目标是建立一套低成本但可信的真实链路质量闭环：

```text
上传或创建资料
-> 简历诊断与 ResumeProfile 写入
-> CareerProfile merge
-> 粘贴 JD 转 SessionArtifact
-> JDAnalysis 写入
-> JobFitReport 写入
-> ResumeVersion 创建
-> CareerProductStore 一致性检查
-> 前端能读到资产和预览 artifact
```

完成后应能回答三个问题：

1. 真实模型跑出来的产品记录是否完整。
2. 产品记录和 artifact 引用是否一致。
3. 失败时能否快速定位是工具、agent 契约、store、artifact 还是前端读取问题。

## 4. 本阶段不做

- 不做大批量压力测试。
- 不默认跑 `runs=3` 或更高。
- 不默认并发跑 live smoke。
- 不引入 LangGraph。
- 不做新的前端视觉大改。
- 不做产品记录编辑。
- 不做跨 store 强一致性迁移。

## 5. Smoke 分层策略

### 5.1 L0：无真实模型检查

用途：每次后端或工具层变更后优先运行。

特点：

- 不消耗模型 token。
- 运行快。
- 主要覆盖模型、store、工具、API、agent 契约和前端解析。

建议命令：

```bash
uv run pytest \
  tests/test_career_product_store.py \
  tests/test_career_tools.py \
  tests/test_career_api.py \
  tests/test_career_agent_flow.py \
  tests/test_career_product_store_checker.py
```

前端相关变更再补：

```bash
cd flutter_app
/home/ubunt/resume_agent/flutter/bin/flutter test \
  test/chat_bubble_markdown_test.dart \
  test/career_assets_panel_test.dart \
  test/run_progress_panel_test.dart \
  test/input_bar_test.dart
```

### 5.2 L1：低成本真实链路 smoke

用途：确认真实模型可以完整走完求职主链路。

默认只跑：

```text
runs=1
concurrency=1
max_tool_rounds=8
```

建议命令：

```bash
uv run python tools/smoke_career_live_flow.py \
  --runs 1 \
  --concurrency 1 \
  --max-tool-rounds 8 \
  --data-dir data/live_career_smoke_quick
```

跑完后立刻执行一致性检查：

```bash
uv run python tools/check_career_product_store.py \
  --data-dir data/live_career_smoke_quick/run_001
```

M6-1 后，`tools/smoke_career_live_flow.py` 已经在每个 run 结束时自动调用 `check_career_product_store`。独立 checker 命令主要用于复验历史 run、定位失败细节，或者在不重新消耗模型 token 的情况下检查已有产物。

通过标准：

- 五类产品记录至少各有一条：`ResumeProfile / CareerProfile / JDAnalysis / JobFitReport / ResumeVersion`。
- 相关报告 artifact 存在且可通过 artifact id 读取。
- smoke 报告中的质量门禁为“通过”，即 `check_career_product_store` 没有 error 级别 finding。
- 最终 `ResumeVersion` 不包含“占位 / 待替换”表达，不包含源材料未明确提供的高风险量化指标。
- 没有工具调用参数路径泄漏，例如 `path`、`file_path`、`workspace_path`。
- 没有 `Tool call limit reached`。

### 5.3 L2：小批次回归 smoke

用途：只有在 L1 通过，并且刚修过 agent 契约或工具稳定性问题时才跑。

建议上限：

```text
runs=2
concurrency=1
max_tool_rounds=8
```

命令：

```bash
uv run python tools/smoke_career_live_flow.py \
  --runs 2 \
  --concurrency 1 \
  --max-tool-rounds 8 \
  --data-dir data/live_career_smoke_regression
```

暂时不建议 `concurrency > 1`。并发会放大模型耗时、工具轮次和数据排查成本，不适合当前 token 紧张阶段。

### 5.4 L3：压力测试

M6 暂不执行。

触发条件必须同时满足：

- L0 稳定。
- L1 连续多次稳定。
- 已经修完主链路已知问题。
- 明确需要评估并发性能或极限失败模式。

否则不跑压力测试。

## 6. Smoke 结果记录规则

每次真实 smoke 只记录必要信息，避免长篇复制模型输出：

```text
日期时间
命令
run 数 / concurrency / max_tool_rounds
总耗时
每轮是否成功
产品记录数量
artifact 数量
失败工具名
关键错误
一致性检查结果
质量门禁结果
下一步修复项
```

不要把完整回答粘进文档。需要复盘时读 `data_dir` 下的事件和 artifact。

## 7. 主线缺口清单

### 7.1 真实链路稳定性

需要继续关注：

- 定制简历阶段是否会错误地再次委派 child-agent。
- 是否会重复创建 `ResumeProfile / JDAnalysis / JobFitReport`。
- 是否会猜测产品记录 id。
- 是否会触发工具轮次上限。
- 粘贴 JD 是否总是先创建 text artifact。
- `JobFitReport.source_artifact_id` 是否指向 JD artifact，报告输出是否使用 `report_artifact_id`。

### 7.2 数据一致性

需要继续关注：

- 产品记录的 `source_session_id` 是否正确。
- `source_artifact_id` 是否存在于对应 session。
- `evidence_refs` 只做格式校验，不做跨 store 强耦合校验。
- `CareerProfile` merge 是否保留 evidence，不做简单覆盖。
- 多次上传同一简历后，前端是否区分当前记录和历史记录。

### 7.3 前端读取链路

需要继续关注：

- 右侧资产面板是否能列出最新记录。
- 历史记录是否能打开对应 artifact 预览。
- 报告 artifact 和产品记录详情是否能互相定位。
- 内部 ID 是否只出现在次级调试入口，不作为主要用户文案。

## 8. 下一批开发顺序

### 8.1 第一批：主线质量记录

新增或更新一份 smoke 结果记录文档，按第 6 节格式记录每次 L1/L2 结果。

建议文件：

```text
求职Agent_M6_smoke结果记录_2026-05-11.md
```

### 8.2 第二批：轻量失败归因

如果 L1 失败，优先补 `tools/smoke_career_live_flow.py` 的失败摘要，而不是增加运行批次。

目标：

- 失败时直接输出阶段名。
- 输出失败工具名。
- 输出是否缺少某类产品记录。
- 输出最后 8 条关键事件。
- 输出一致性检查 finding。
- 输出质量门禁是否通过，以及 error code 摘要。

### 8.3 第三批：只修真实主链路问题

只修影响主链路稳定的问题，例如：

- agent 契约导致重复委派。
- 工具参数兼容性不足。
- 产品记录引用不一致。
- artifact 预览取错版本。
- 前端读取当前记录和历史记录关系不清。

不为了通过某一次 smoke 做定点硬编码。

## 9. 建议下一步

下一步先不跑 smoke，也不继续前端视觉优化。

推荐先做：

```text
1. 新建 smoke 结果记录文档模板。
2. 检查 tools/smoke_career_live_flow.py 的失败摘要是否足够短、足够可定位。
3. 如不足，只增强摘要输出，不改变主链路逻辑。
4. 等需要验证时，再跑 L1：runs=1 concurrency=1。
```

这样可以把 token 消耗控制住，同时不丢主线质量闭环。
