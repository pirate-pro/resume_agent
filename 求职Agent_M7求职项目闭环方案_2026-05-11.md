# 求职 Agent M7 求职项目闭环方案

日期：2026-05-11

## 1. 背景

M1 到 M6 已经把底层求职资产打通：

```text
ResumeProfile
CareerProfile
JDAnalysis
JobFitReport
ResumeVersion
SessionArtifact
AgentTask
events
```

这些资产现在能够被创建、读取、预览和检查质量，但对用户来说仍然偏“资料列表”。用户真正关心的不是内部有多少类记录，而是：

```text
我正在投哪个岗位？
这个岗位匹配度如何？
我准备用哪版简历投？
下一步该准备什么？
这个岗位现在到哪个阶段了？
```

所以 M7 的目标不是继续增加零散能力，而是引入一个产品级容器，把“投一个岗位”组织成清晰闭环。

## 2. M7 一句话目标

```text
从“生成多个求职资产”升级为“围绕一个目标岗位形成一个可追踪、可继续推进的求职项目”。
```

## 3. 核心新增概念：求职项目

M7 新增 `CareerApplication`，中文产品名建议叫：

```text
求职项目
```

它不是替代现有资产，而是把现有资产组织起来。

```text
CareerApplication
  -> ResumeProfile
  -> CareerProfile
  -> JDAnalysis
  -> JobFitReport
  -> ResumeVersion
  -> SessionArtifact
```

用户看到的是“求职项目”，点进去再看简历画像、JD 分析、匹配报告、定制简历等资料。

## 4. 为什么现在要做它

当前产品已经具备“产出资产”的能力，但还缺少“围绕目标岗位持续推进”的主线。

没有 `CareerApplication` 时会出现几个问题：

1. 右侧资产面板会越来越像内部记录列表，用户不容易理解。
2. 多份简历、多份 JD、多份匹配报告之间的关系不够直观。
3. 用户下一次回来时，不知道哪个岗位进行到哪里。
4. 后续面试准备、投递状态、复盘记录没有挂载点。
5. 前端很难形成“项目卡片 -> 项目详情 -> 下一步动作”的产品路径。

`CareerApplication` 的价值是把产品从“工具集合”变成“求职工作台”。

## 5. M7 范围

### 5.1 本阶段做

- 新增 `CareerApplication` 模型。
- 在 `CareerProductStore` 中支持保存、读取、列表、归档、合并更新。
- 允许从已有 `JDAnalysis / JobFitReport / ResumeVersion` 组装求职项目。
- 增加工具层，让 agent 可以创建和更新求职项目。
- 增加 API，让前端可以读取求职项目。
- 前端右侧资产栏优先展示“求职项目”，散装资产放到二级入口。
- 对话结果里优先引导用户查看或继续推进某个求职项目。

### 5.2 本阶段不做

- 不做面试复盘模型。
- 不做求职笔记模型。
- 不做 memory 扩展。
- 不做复杂投递 CRM。
- 不做看板拖拽。
- 不做多用户权限。
- 不做自动投递。
- 不做外部招聘平台集成。

面试和笔记后面会接到求职项目上，但 M7 先把“一个岗位的求职闭环”跑顺。

## 6. 模型设计

### 6.1 CareerApplication 字段

建议第一版字段：

```text
application_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

company
position
location
job_url
stage
priority

resume_profile_id
career_profile_id
jd_analysis_id
job_fit_report_id
resume_version_ids

summary
next_actions
risks
notes
```

### 6.2 字段说明

`application_id`

受控 ID，格式建议：

```text
application_xxx
```

`status`

沿用现有记录状态：

```text
active | archived
```

`stage`

表示求职项目当前阶段，第一版建议固定枚举：

```text
draft
analyzing
ready_to_apply
applied
interviewing
offer
rejected
paused
```

中文含义：

```text
draft：草稿，刚创建项目
analyzing：正在分析
ready_to_apply：已准备投递
applied：已投递
interviewing：面试中
offer：已收到 offer
rejected：已结束
paused：暂缓
```

`priority`

第一版使用简单枚举：

```text
high | medium | low
```

不做复杂评分排序。匹配分数仍然来自 `JobFitReport`。

`source_artifact_id`

优先指向 JD artifact。因为一个求职项目通常围绕一个目标岗位创建。

`evidence_refs`

继续使用字符串引用，只校验格式，不做跨 store 强存在性校验。M7 不引入 typed ref，避免范围膨胀。

`resume_version_ids`

允许一个求职项目挂多版简历，例如：

```text
初版
偏后端版
偏 Agent 平台版
投递最终版
```

第一版只负责保存列表，不做版本 diff。

`summary`

给用户看的项目摘要，例如：

```text
星河智能 · AI Agent 后端工程师，匹配度 72/100，建议谨慎投递，重点补 RAG 实战和 LangGraph/LangChain 经验说明。
```

`next_actions`

下一步建议，必须是用户能执行的动作，例如：

```text
补充 RAG 项目细节
生成投递版简历
准备 Agent 架构面试问题
确认是否已经投递
```

`risks`

挂项目级风险，不替代 `JobFitReport` 的详细风险。

`notes`

M7 只保留轻量文本备注字段，不做独立笔记系统。它不是 memory，也不是长期笔记。

## 7. 存储结构

建议新增目录：

```text
data/career/applications/{application_id}.json
```

不要把 `CareerApplication` 塞进 session 仓储。

M7 后，`CareerProductStore` 继续作为求职产品资产事实源：

```text
CareerProductStore
  -> ResumeProfile
  -> CareerProfile
  -> JDAnalysis
  -> JobFitReport
  -> ResumeVersion
  -> CareerApplication
```

## 8. Store 行为

新增方法：

```text
save_career_application(record)
get_career_application(application_id)
list_career_applications(include_archived=False)
archive_career_application(application_id)
merge_career_application(application_id, updates, evidence_refs, source_artifact_id=None)
```

### 8.1 时间戳规则

沿用当前 store 规则：

- 新建时由 store 设置 `created_at / updated_at`。
- 更新时保留原 `created_at`，只更新 `updated_at`。
- 外部传入时间不作为可信时间。

### 8.2 merge 规则

`merge_career_application` 只允许更新这些字段：

```text
stage
priority
resume_profile_id
career_profile_id
jd_analysis_id
job_fit_report_id
resume_version_ids
summary
next_actions
risks
notes
```

不允许更新：

```text
application_id
created_at
updated_at
status
source_session_id
```

列表字段使用合并语义，不简单覆盖：

```text
resume_version_ids
next_actions
risks
```

`notes` 第一版可以覆盖，因为它只是项目轻量备注，不承担审计职责。

## 9. 工具设计

M7 新增工具：

```text
career_application_create
career_application_get
career_application_list
career_application_merge
```

### 9.1 create 工具

用于创建求职项目。

输入建议：

```json
{
  "company": "星河智能",
  "position": "AI Agent 后端工程师",
  "source_artifact_id": "artifact_xxx",
  "jd_analysis_id": "jd_xxx",
  "job_fit_report_id": "fit_xxx",
  "resume_profile_id": "resume_profile_xxx",
  "career_profile_id": "career_profile_default",
  "resume_version_ids": ["resume_version_xxx"],
  "summary": "...",
  "next_actions": ["..."],
  "risks": ["..."],
  "evidence_refs": ["artifact_xxx", "jd_xxx", "fit_xxx"]
}
```

### 9.2 merge 工具

用于推进项目阶段或补充资产引用。

典型场景：

```text
生成了新简历版本 -> 合并 resume_version_id
用户说已投递 -> stage 改为 applied
用户说约了面试 -> stage 改为 interviewing
匹配报告发现风险 -> 合并 risks / next_actions
```

## 10. Agent 契约

### 10.1 main-agent

main-agent 是 `CareerApplication` 的主要创建者和维护者。

规则：

- 当用户同时提供简历和 JD，或要求“帮我投这个岗位 / 匹配这个岗位 / 生成定制简历”时，最终应创建或更新一个 `CareerApplication`。
- 已经存在可用项目时，优先更新现有项目，不要重复创建。
- 不确定项目 ID 时，先调用 `career_application_list`。
- 不能猜测 `application_id`。
- `CareerApplication` 只保存产品项目状态和资产引用，不保存大段报告正文。

### 10.2 resume_agent

第一版不需要直接写 `CareerApplication`。

它继续负责：

```text
ResumeProfile
诊断 artifact
```

### 10.3 job_agent

第一版可以不直接写 `CareerApplication`。

它继续负责：

```text
JDAnalysis
JobFitReport
报告 artifact
```

项目创建由 main-agent 汇总完成，边界更清晰。

## 11. API 设计

新增接口：

```text
GET /api/career/applications
GET /api/career/applications/{application_id}
```

第一版只做读取。创建和更新仍然通过 agent 工具完成，避免前端提前承担业务编排。

响应字段应包含：

```text
meta
application_id
company
position
location
job_url
stage
priority
resume_profile_id
career_profile_id
jd_analysis_id
job_fit_report_id
resume_version_ids
summary
next_actions
risks
notes
```

## 12. 前端形态

### 12.1 右侧面板信息架构

M7 后，右侧面板建议从“求职资产”升级为：

```text
求职工作台
```

第一层优先展示：

```text
求职项目
```

第二层再展示：

```text
简历画像
职业画像
JD 分析
匹配报告
简历版本
Artifact
```

### 12.2 项目卡片

项目卡片应展示用户真正关心的信息：

```text
公司 + 岗位
当前阶段
匹配度
推荐等级
下一步动作
更新时间
```

示例：

```text
星河智能 · AI Agent 后端工程师
阶段：已准备投递
匹配度：72/100
建议：谨慎推荐
下一步：补充 RAG 项目细节，生成最终投递版简历
```

### 12.3 项目详情

点击项目卡片后展示：

```text
项目概览
匹配结论
关键风险
下一步动作
关联资料
```

关联资料包括：

```text
简历画像
JD 分析
匹配报告
简历版本
源 JD artifact
报告 artifact
```

用户不应该先看到一堆内部 ID。内部 ID 继续放在调试级入口。

## 13. smoke 与质量检查

M7 后，低成本 smoke 通过标准增加一条：

```text
至少创建或更新一个 CareerApplication。
```

质量检查增加：

- `CareerApplication.source_artifact_id` 指向 JD artifact 时必须是合法 artifact id。
- `resume_profile_id / jd_analysis_id / job_fit_report_id / resume_version_ids` 只做格式校验。
- 不做跨 store 存在性强校验，避免 M7 范围膨胀。
- `summary` 不能包含“占位 / 待替换”。
- `stage / priority` 必须在枚举内。

## 14. 开发顺序

### 14.1 第一批：后端模型和 store

```text
app/career/models.py
app/career/store.py
tests/test_career_product_store.py
```

目标：

- 新增 `CareerApplication`。
- 新增 `application_id` 校验。
- 新增 `stage / priority` 校验。
- 新增 save/get/list/archive/merge。
- 保持原子写入。
- 损坏 JSON 稳定失败。

### 14.2 第二批：checker

```text
tools/check_career_product_store.py
tests/test_career_product_store_checker.py
```

目标：

- 检查 `CareerApplication` 基础合法性。
- 检查引用格式。
- 检查 summary 占位表达。
- 输出 counts 增加 `career_applications`。

### 14.3 第三批：工具

```text
app/tools/builtin_tools/career.py
tests/test_career_tools.py
```

目标：

- 新增 create/get/list/merge 工具。
- 工具只接受受控 ID，不接受路径。
- create 支持从 JDAnalysis/JobFitReport 推导 company/position。
- merge 要求 evidence_refs。

### 14.4 第四批：agent 契约

```text
app/agents/default/AGENT.md
app/runtime/agent_capability.py
tests/test_career_agent_flow.py
```

目标：

- main-agent 知道什么时候创建求职项目。
- child-agent 不直接维护求职项目。
- 定制简历后要把 `resume_version_id` 合并进求职项目。

### 14.5 第五批：API 和前端读取

```text
app/schemas/career.py
app/api/presenters.py
app/api/career.py
flutter_app/lib/core/models/api_models.dart
flutter_app/lib/core/services/api_service.dart
flutter_app/lib/core/providers/career_assets_provider.dart
flutter_app/lib/features/career/career_assets_panel.dart
```

目标：

- API 返回求职项目列表。
- 前端右侧面板优先展示求职项目。
- 散装资产仍可查看，但不再作为第一产品入口。

### 14.6 第六批：低成本真实链路验证

```text
tools/smoke_career_live_flow.py
```

目标：

- L1 smoke 检查至少一个 `CareerApplication`。
- 不增加 runs。
- 不做压力测试。

## 15. M7 完成标准

M7 完成后，用户应该能完成这条路径：

```text
上传简历
粘贴 JD
系统生成简历画像、JD 分析、匹配报告、定制简历
系统创建一个求职项目
用户在右侧看到“星河智能 · AI Agent 后端工程师”项目卡片
用户点进去能看到匹配结论、下一步动作和关联资料
```

后端完成标准：

- `CareerApplication` 进入 `CareerProductStore`。
- 工具、API、checker、smoke 全部识别 `CareerApplication`。
- main-agent 能创建和更新求职项目。
- 不破坏现有五类资产。

前端完成标准：

- 右侧面板以求职项目为主入口。
- 项目详情能打开关联报告和简历版本。
- 内部 ID 不作为主要展示文案。

## 16. 风险与控制

### 16.1 范围膨胀风险

最容易膨胀的是面试、笔记、CRM、memory。

控制方式：

```text
M7 只做求职项目容器。
面试准备只作为 next_actions，不建 Interview 模型。
笔记只保留 notes 文本字段，不建 Note 模型。
memory 不参与 M7 数据写入。
```

### 16.2 重复项目风险

同一 JD 可能被多次分析。

M7 第一版不做复杂去重，只建议工具层在创建前 list 一次，按以下组合做弱判断：

```text
source_session_id
source_artifact_id
company
position
```

如果已有相近项目，优先 merge。

### 16.3 前端复杂度风险

资产面板已经较复杂，直接重构风险较高。

控制方式：

```text
先新增“求职项目”分组。
保留原来的资产列表。
确认体验稳定后，再把散装资产降级到二级入口。
```

## 17. 建议下一步

先审核本方案，重点看：

1. `CareerApplication` 字段是否足够表达一个目标岗位。
2. `stage / priority` 枚举是否符合第一版产品。
3. 是否同意 main-agent 维护求职项目、child-agent 不直接维护。
4. 是否同意 M7 暂不做面试复盘、笔记、memory 扩展。

审核通过后，第一批只开发：

```text
app/career/models.py
app/career/store.py
tests/test_career_product_store.py
```

先把求职项目底座做稳，再接工具、agent 和前端。
