# 求职 Agent 下一里程碑执行方案

日期：2026-05-07

## 1. 目标

本文档定义下一个产品实现里程碑：**M1 产品记录层与求职闭环**。

M1 的目标不是复述或压缩产品方向文档，而是给当前代码分支确定一个可以开发、测试、验收的推进单元。完成 M1 后，后续功能都应该围绕已落地的产品记录继续扩展，而不是继续只堆运行时能力。

M1 要打通的产品闭环是：

```text
上传简历
  -> 解析为结构化简历画像
  -> 生成或更新职业画像
  -> 粘贴或上传一个 JD
  -> 分析岗位匹配度
  -> 创建一个定制简历版本
  -> 在聊天之外可以查看和复用这些产品资产
```

完成 M1 后，系统应该从“通用聊天运行时”进一步变成“可持续使用的个人求职工作台”：用户上传的简历、职业画像、JD 分析、匹配报告和简历版本都能作为稳定产品资产沉淀下来。

## 2. M1 范围

### 2.1 本里程碑包含

- 从当前会话 artifact 中读取简历并做结构化提取。
- 生成和更新用户职业画像。
- 支持 JD 输入、JD 解析和岗位匹配分析。
- 生成一个面向目标岗位的简历优化版本。
- 复用现有 `agent_main`、`resume_agent`、`job_agent` 做多 Agent 编排。
- 继续坚持 artifact-first 的跨 Agent 资料传递方式。

### 2.2 本里程碑不包含

- 外部岗位库、公司库、面经库。
- 向量数据库。
- Web 爬取。
- 高质量 PDF/DOCX 导出。
- 后台异步 `delegate_agents(wait=false)`。
- 现有 memory / mid-term flush 机制调整。
- 新增 `InterviewAgent`、`ReviewAgent`、`DiaryMemoryAgent` 等更多专职 Agent。
- 求职笔记、聊天摘录、面试日记等 notes 系统。
- 完整前端重构。
- 账号、鉴权、计费、多用户隔离。

## 3. 架构边界

M1 新增的是求职产品资产层，不替代现有运行时事实源。

当前事实源边界：

- `SessionArtifact`：上传文件和生成文件的事实源。
- `AgentTask`：multi-agent 委派任务生命周期的事实源。
- `events`：运行审计日志。
- `memory`：长期/中期上下文材料，不承担产品资产职责。
- `workspace`：Agent 私有中间产物，不作为跨 Agent 资料源。

M1 新增事实源：

```text
CareerProductStore
  -> ResumeProfile
  -> CareerProfile
  -> JDAnalysis
  -> JobFitReport
  -> ResumeVersion
```

关键边界：

- `CareerProductStore` 必须独立于 `SessionRepository`、`MemoryStore`、`AgentTaskStore`。
- `CareerProfile` 是用户可见、可编辑、可复用的产品资产，不存进 `memory long_term`。
- artifact 仍然是文件事实源；简历原文、JD 原文、生成的 markdown 简历版本都应该保存为 session artifact。
- 产品记录只保存结构化字段和 artifact 引用。
- 产品工具只接受 `resume_profile_id`、`jd_analysis_id`、`artifact_id` 等受控 id，不暴露任意文件路径。
- 用户粘贴的 JD 也必须先创建 text 类型 `SessionArtifact`，再进入 `JDAnalysis` 流程。
- `diagnosis.md`、`job_fit_report.md`、定制简历 markdown 只要面向用户可查看或可复用，就应该生成 session artifact；产品记录保存对应 `artifact_id`。
- M1 不创建 `data/career/.../*.md` 内部快照，避免和 session artifact 形成双事实源。

## 4. M1 用户流程

### 4.1 首次简历诊断

```text
用户上传简历
用户询问：“帮我看看我适合投什么岗位”
MainCareerAgent 委派 ResumeAgent 解析简历
ResumeAgent 读取 artifact 并发布结构化简历画像
MainCareerAgent 更新职业画像
MainCareerAgent 返回简历诊断，并追问目标 JD 或目标方向
```

预期持久化资产：

```text
resume_profile.json
resume_diagnosis_artifact
career_profile.json
```

### 4.2 JD 匹配分析

```text
用户粘贴或上传 JD
MainCareerAgent 先把粘贴 JD 转成 text artifact
MainCareerAgent 让 JobAgent 分析 JD 和岗位匹配度
JobAgent 读取简历画像、职业画像和 JD artifact
JobAgent 发布 JD 分析和岗位匹配报告
MainCareerAgent 汇总匹配度、风险和下一步建议
```

预期持久化资产：

```text
jd_source_artifact
jd_analysis.json
job_fit_report_artifact
```

### 4.3 定制简历优化

```text
用户询问：“基于这个 JD 帮我优化简历”
MainCareerAgent 构造定制简历改写任务
JobAgent 或主运行时生成定制简历草稿
Agent 发布 markdown 简历版本
MainCareerAgent 解释关键改动和取舍
```

预期持久化资产：

```text
resume_version_artifact
resume_version_meta.json
```

## 5. Agent 形态

M1 不增加更多 Agent 身份。先使用三个 Agent，并通过产品语义工具扩展能力。

### 5.1 `agent_main`

定位：

- 面向用户的总控 Agent。
- 理解用户意图。
- 委派简历和 JD 相关子任务。
- 读取子 Agent 结果。
- 更新职业画像。
- 生成最终面向用户的回答。

允许工具：

- 读取所有 career 产品记录。
- 更新 `CareerProfile`。
- 创建最终 `ResumeVersion`。
- 使用现有通用编排和 artifact 工具。

### 5.2 `resume_agent`

定位：

- 读取简历 artifact。
- 提取结构化简历画像。
- 诊断简历缺失项和薄弱点。
- 发布机器可读 JSON 和人类可读诊断报告。

边界：

- M1 不负责独立判断长期职业策略。
- 可以写 `ResumeProfile` 和诊断相关 artifact。
- 不写 `JDAnalysis`、`JobFitReport`、`ResumeVersion`。
- 不更新 `CareerProfile`。

### 5.3 `job_agent`

定位：

- 解析 JD。
- 对照简历画像和职业画像分析匹配度。
- 输出岗位匹配分析和简历优化方向。
- 可选地生成定制简历版本草稿。

边界：

- 只读取 session 共享 artifact 和受控产品画像工具。
- 不能直接访问其他 Agent 的 workspace。
- 可以读取 `ResumeProfile` 和 `CareerProfile`。
- 可以写 `JDAnalysis` 和 `JobFitReport`。
- 不更新 `CareerProfile`。
- 不创建最终 `ResumeVersion`，除非后续明确把该职责下放。

## 6. 产品数据模型

这些模型应被视为产品资产，而不是普通聊天文本。

所有产品记录都必须包含基础元数据：

```json
{
  "status": "active | archived",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": [],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z"
}
```

规则：

- `status` 用于后续多份简历、多份 JD、多份简历版本的归档管理。
- `source_session_id` 用于追溯记录来自哪个会话。
- `source_artifact_id` 指向主要文件事实源；没有单一文件来源时可以为空，但必须提供 `evidence_refs`。
- M1 的 `evidence_refs` 先保存受控字符串引用，并做前缀校验，例如 `artifact_xxx`、`resume_profile_xxx`、`jd_xxx`、`fit_xxx`、`sess_xxx`。
- M1 不做跨记录存在性校验；只校验引用字符串格式，避免 career store 和其他 store 过早耦合。
- 后续可以升级为 typed ref，例如 `{"type": "artifact", "id": "artifact_xxx"}`；M1 不引入该结构，避免扩大第一步实现范围。
- `created_at` 和 `updated_at` 由 store 统一维护。

### 6.1 ResumeProfile

用途：

- 保存用户简历的长期结构化表达。
- 支撑职业画像、JD 匹配、面试准备和简历优化。

建议 JSON：

```json
{
  "resume_profile_id": "resume_profile_xxx",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["artifact_xxx"],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "basic_info": {
    "name": "",
    "email": "",
    "phone": "",
    "location": ""
  },
  "education": [],
  "work_experience": [],
  "project_experience": [],
  "skills": [],
  "certificates": [],
  "awards": [],
  "self_evaluation": "",
  "raw_text_artifact_id": "artifact_xxx",
  "diagnosis_artifact_id": "artifact_xxx",
  "diagnosis": {
    "strengths": [],
    "weaknesses": [],
    "missing_sections": [],
    "rewrite_opportunities": []
  }
}
```

### 6.2 CareerProfile

用途：

- 保存用户长期职业画像。
- 汇总目标、优势、风险、偏好和面试薄弱点。

建议 JSON：

```json
{
  "career_profile_id": "career_profile_default",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": null,
  "evidence_refs": [],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "career_goal": "",
  "target_roles": [],
  "preferred_industries": [],
  "preferred_cities": [],
  "strengths": [],
  "weaknesses": [],
  "skills": [],
  "interests": [],
  "education_summary": "",
  "experience_summary": "",
  "resume_issues": [],
  "interview_weaknesses": []
}
```

更新规则：

- `career_profile_update` 只支持 merge 语义，不提供整体覆盖能力。
- 每次 merge 必须传入 `evidence_refs`，说明本次更新来自哪些简历画像、JD 分析、artifact 或 session。
- 空字段不覆盖已有有效字段。
- 列表字段默认去重追加；确需删除或归档时后续单独设计显式操作。

### 6.3 JDAnalysis

用途：

- 保存某个岗位描述的结构化分析。
- 支撑岗位匹配、简历改写和面试准备。

建议 JSON：

```json
{
  "jd_analysis_id": "jd_xxx",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["artifact_xxx"],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "company": "",
  "position": "",
  "seniority": "",
  "required_skills": [],
  "preferred_skills": [],
  "responsibilities": [],
  "keywords": [],
  "risk_signals": [],
  "interview_focus": []
}
```

### 6.4 JobFitReport

用途：

- 保存用户画像和某个 JD 之间的一次匹配报告。
- `source_artifact_id` 指输入主来源，优先指向 JD artifact。
- 用户可见报告输出使用 `report_artifact_id`，不要和 `source_artifact_id` 混用。

建议 JSON：

```json
{
  "job_fit_report_id": "fit_xxx",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["resume_profile_xxx", "career_profile_default", "jd_xxx", "artifact_xxx"],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "jd_analysis_id": "jd_xxx",
  "resume_profile_id": "resume_profile_xxx",
  "career_profile_id": "career_profile_default",
  "overall_score": 0,
  "score_breakdown": {
    "skills": 0,
    "projects": 0,
    "education": 0,
    "interest": 0,
    "growth_potential": 0,
    "interview_readiness": 0
  },
  "matched_evidence": [],
  "gaps": [],
  "resume_optimization_direction": [],
  "interview_preparation_focus": [],
  "recommendation": "recommended | cautious | not_recommended",
  "report_artifact_id": "artifact_xxx"
}
```

### 6.5 ResumeVersion

用途：

- 保存面向某个岗位或 JD 的简历版本。

建议 JSON：

```json
{
  "resume_version_id": "resume_version_xxx",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["resume_profile_xxx", "jd_xxx", "fit_xxx", "artifact_xxx"],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "base_resume_profile_id": "resume_profile_xxx",
  "target_jd_analysis_id": "jd_xxx",
  "title": "Agent/RAG 应用开发简历版本",
  "format": "markdown",
  "artifact_id": "artifact_xxx",
  "change_summary": [],
  "keyword_strategy": [],
  "risk_notes": []
}
```

## 7. 存储建议

继续沿用当前后端的 file-first 风格。产品数据需要和原始 session events 分离。

推荐目录：

```text
data/
  career/
    profiles/
      career_profile_default.json
    resumes/
      resume_profile_xxx/
        profile.json
      versions/
        resume_version_xxx.json
    jobs/
      jd_xxx/
        jd_analysis.json
        job_fit_report.json
```

原因：

- session artifacts 继续作为上传文件和生成文件的事实源。
- 产品记录提供跨 session 可复用资产。
- Agent 需要稳定结构化读取的内容使用 JSON。
- 本阶段不新增 notes 目录，避免扩大 M1 范围。
- `CareerProductStore` 是新的 domain store，不挂到 session 仓储里。
- M1 不实现 `data/career/.../*.md` 内部快照，避免 markdown 双事实源。
- 对外可见的 markdown 报告和简历版本以 session artifact 为准。

M1 简化：

- 先按单本地用户处理。
- 只有一个默认 `career_profile_default`。
- 不做旧数据迁移。
- 不新增独立笔记存储。
- store 第一阶段只实现 JSON 产品记录的模型校验、ID 约束、原子写入、读写列表、归档状态和损坏 JSON 失败处理。

## 8. 新产品工具

这些工具应该窄而稳定，表达产品语义，不向模型暴露任意文件路径。

通用规则：

- 工具参数只允许受控 id，例如 `resume_profile_id`、`jd_analysis_id`、`job_fit_report_id`、`resume_version_id`、`artifact_id`。
- 工具内部负责解析 id 到具体存储路径。
- 不允许 Agent 传入任意文件路径。
- 不把所有 career 工具直接开放给所有 Agent，按第 5 节职责授权。

### 8.1 简历工具

```text
resume_profile_save
resume_profile_get
resume_profile_list
resume_version_create
resume_version_get
```

职责：

- 持久化 `ResumeProfile`。
- 读取最新或指定简历画像。
- 保存生成的简历版本元数据和 artifact id。

### 8.2 职业画像工具

```text
career_profile_get
career_profile_update
career_profile_extract_from_resume
```

职责：

- 维护一个长期职业画像。
- 只支持 merge 更新，不盲目覆盖已有事实。
- 保留 resume、JD、session artifact 等证据引用。

### 8.3 JD 和匹配工具

```text
jd_analysis_save
jd_analysis_get
job_fit_report_save
job_fit_report_get
```

职责：

- 持久化 JD 分析。
- 持久化岗位匹配报告。
- 让 Agent 不需要反复从聊天历史中重新解析 JD。

## 9. API 建议

第一版后端 API 可以保持轻量，和产品工具语义对齐。

### 9.1 简历

```text
GET  /api/career/resumes
GET  /api/career/resumes/{resume_profile_id}
GET  /api/career/resume-versions
GET  /api/career/resume-versions/{resume_version_id}
```

简历创建可以先通过聊天和 Agent 工具完成。

### 9.2 职业画像

```text
GET   /api/career/profile
PATCH /api/career/profile
```

### 9.3 岗位

```text
GET /api/career/jobs
GET /api/career/jobs/{jd_analysis_id}
GET /api/career/job-fit-reports/{job_fit_report_id}
```

## 10. Prompt 和 Agent 契约调整

### 10.1 MainCareerAgent

新增要求：

- 优先创建持久化产品记录，而不是只在聊天里回答。
- 在简历、JD、画像流程中说明创建或更新了哪些持久化资产。
- 只有关键数据缺失时才追问用户。
- M1 重点是创建和复用产品记录，不新增 memory 相关行为要求。

### 10.2 ResumeAgent

新增要求：

- 始终同时输出结构化提取结果和诊断结论。
- 使用 `resume_profile_save` 保存结构化数据。
- 发布一份人类可读的简历诊断 artifact。
- 明确说明提取不确定的字段。

### 10.3 JobAgent

新增要求：

- 将 JD 解析为结构化要求。
- 对照简历画像和职业画像做匹配分析。
- 保存 JD 分析和岗位匹配报告。
- 当证据不足时，推荐结论保持保守。

## 11. 开发顺序

按实现依赖从底向上推进，不急着改 prompt。

```text
1. app/career/models.py
2. app/career/store.py
3. tests/test_career_product_store.py
4. app/tools/builtin_tools/career.py
5. tests/test_career_tools.py
6. capability 配置
7. AGENT.md 调整
8. 端到端流程测试
```

### 11.1 领域层和 Store

- 新增 `app/career/models.py`。
- 新增 `app/career/store.py`。
- 新增 `tests/test_career_product_store.py`。
- 先覆盖 `ResumeProfile`、`CareerProfile`、`JDAnalysis`。
- 随后补齐 `JobFitReport`、`ResumeVersion`。
- 暂不修改 prompt、工具和 capability，先把领域层独立跑稳。

完成标准：

- 不经过聊天运行时也能保存和读取产品记录。
- 非法 id、缺失字段和损坏 JSON 能稳定失败。
- 写入使用临时文件替换，避免半写入损坏正式记录。
- 所有记录都有 `status`、`source_session_id`、`evidence_refs`、`created_at`、`updated_at`。
- `evidence_refs` 使用字符串前缀校验，不做跨记录存在性校验，并预留后续 typed ref 升级空间。
- `CareerProfile` 更新只支持 merge 语义，并要求提供 `evidence_refs`。
- store 独立于 `SessionRepository`、`MemoryStore`、`AgentTaskStore`。

### 11.2 产品工具

- 注册简历、画像、JD 和匹配报告工具。
- 工具参数只接受受控 id。
- 新增 `tests/test_career_tools.py`。
- 粘贴 JD 进入分析前必须先创建 text artifact，工具只接收 `artifact_id`。
- 报告类输出如果用户可见，必须先生成 session artifact，再把 `artifact_id` 写入产品记录。
- `JobFitReport.source_artifact_id` 指 JD artifact，`report_artifact_id` 指报告 artifact。

完成标准：

- Agent 可以通过工具创建和读取产品记录。
- 工具不允许路径穿越或任意文件写入。

### 11.3 Capability 配置

- 按 Agent 职责开放 career 工具。
- 不使用 `"*"` 一次性开放给所有 Agent。
- `resume_agent` 只获得简历解析相关工具。
- `job_agent` 获得 JD 分析和匹配报告相关工具。
- `agent_main` 获得汇总、职业画像更新和最终简历版本相关工具。

完成标准：

- 工具权限和第 5 节 Agent 边界一致。

### 11.4 Agent 契约

- 更新 `app/agents/default/AGENT.md`。
- 更新 `app/agents/resume_agent/AGENT.md`。
- 更新 `app/agents/job_agent/AGENT.md`。

完成标准：

- 确定性模型测试可以跑通预期工具调用序列。

### 11.5 端到端聊天流程

- 增加一个“简历上传 -> 画像创建”的集成测试。
- 增加一个“JD 匹配报告创建”的集成测试。
- 增加一个“定制简历版本创建”的集成测试。

完成标准：

- `uv run pytest` 通过。
- session event log 能引用已创建资产。
- 产品记录可以通过 API 或 store 方法列出。

### 11.6 最小前端展示

- 增加职业画像、简历画像、匹配报告和简历版本的读取视图。
- M1 先保持“创建主要由聊天驱动”。

完成标准：

- 用户可以在聊天记录之外查看已创建的产品资产。

## 12. 验收标准

M1 完成时，需要满足：

1. 简历 artifact 可以解析为持久化 `ResumeProfile`。
2. 系统可以生成或更新持久化 `CareerProfile`。
3. 粘贴 JD 会先转成 text session artifact，再生成持久化 `JDAnalysis`。
4. 系统可以生成持久化 `JobFitReport`，并保存用户可见报告 artifact 引用。
5. 系统可以创建至少一个定制 `ResumeVersion` artifact，并在产品记录里保存引用。
6. 产品记录不需要回放 session events 就能读取。
7. 多 Agent 资料传递仍然通过 session artifacts 和受控产品工具完成。
8. `CareerProductStore` 独立于 session、memory、agent task 仓储。
9. career 工具按 Agent 职责授权，不向所有 Agent 全量开放。
10. 所有产品记录都有状态、来源、证据引用和时间戳。
11. M1 不创建 `data/career/.../*.md` 内部快照，用户可见 markdown 以 session artifact 为准。
12. 现有后端测试和类型检查通过。

## 13. 主要风险

### 13.1 Agent 输出重新退化成非结构化文本

风险：

- Agent 可能自然语言回答了用户，但没有创建产品记录。

缓解：

- 工具契约要明确。
- 集成测试不能只断言回答文本，还要断言持久化记录存在。

### 13.2 职业画像更新覆盖已有有效信息

风险：

- 新简历或新对话可能擦掉已有职业画像字段。

缓解：

- 使用 merge 语义。
- 保留 evidence refs。
- 除非工具调用明确要求替换，否则尽量保留已有字段。

### 13.3 过早增加太多 Agent

风险：

- 现在新增 `ProfileAgent`、`InterviewAgent`、`ReviewAgent`、`DiaryMemoryAgent` 会在产品数据层稳定前增加编排复杂度。

缓解：

- M1 保持三个 Agent。
- 产品记录流程稳定后，再拆更多专职 Agent。

## 14. 推荐第一个开发任务

建议先做：

```text
CareerProductStore + ResumeProfile + CareerProfile + JDAnalysis
```

原因：

- 这批能力构成产品骨架，且不会引入 notes 系统的范围膨胀。
- 岗位匹配报告和简历优化可以紧接着补上。
- 当前后端已经具备 artifact 上传、workspace 发布和 Agent 委派，M1 要补齐的是产品记录层。

初始文件目标：

```text
app/career/models.py
app/career/store.py
tests/test_career_product_store.py
```

这个基础完成后，再实现：

```text
JobFitReport
ResumeVersion
```

然后进入工具层、capability 配置、Agent prompt 和端到端测试。
