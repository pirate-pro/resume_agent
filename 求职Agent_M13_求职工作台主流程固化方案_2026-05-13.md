# 求职 Agent M13 求职工作台主流程固化方案

## 1. 一句话目标

把 M12 已验证通过的 Agent 能力，固化成用户可稳定使用的求职工作台主流程。

M13 不再证明“Agent 能不能做”，而是解决：

```text
用户打开产品后，能不能清楚看到自己正在推进哪些岗位，
每个岗位现在到哪一步，
还缺什么，
下一步该做什么，
并且不用复制 ID、不用猜提示词，就能继续推进。
```

## 2. 当前基础

当前后端已经具备这些事实源：

```text
SessionArtifact = 文件事实源
CareerProductStore = 求职产品资产事实源
NoteService = 用户可见笔记资产
LearningService = 学习计划、任务、打卡和短板跟踪
KnowledgeService = 外部资料、面经、题库和公司要求
RetrievalService = 只读召回层
AgentTask = 多 agent 委派执行事实源
events = 运行审计和前端进度来源
memory = 长期上下文材料，不承担产品资产职责
```

M12 已完成四条低批次真实链路验收：

```text
interview_prep：先召回，再只读回答
learning_task：先召回，再创建 LearningPlan / LearningTask
save_note：先召回，再创建 Note
pre_apply_check：先召回，再更新 CareerApplication
```

这些能力现在还主要散在对话、资产栏和工具结果里。M13 要把它们组织成用户可理解的工作台。

## 3. M13 不做什么

M13 不做这些事：

- 不引入新的 Agent。
- 不引入 embedding。
- 不接 MCP。
- 不做 memory 自动写入。
- 不做日历提醒。
- 不做学习资料 RAG 深加工。
- 不重构现有 Career / Note / Learning / Knowledge store。
- 不把前端做成复杂项目管理系统。

M13 的重点是产品主流程，不是底层技术扩张。

## 4. 用户主路径

M13 第一版围绕一个核心入口：

```text
求职项目工作台
```

用户路径：

```text
上传简历 / 输入 JD
  -> Agent 创建简历画像、JD 分析、匹配报告、求职项目
  -> 用户进入某个求职项目工作台
  -> 查看当前岗位匹配度、风险、下一步行动、关联资产
  -> 一键触发：生成定制简历 / 面试准备 / 保存笔记 / 创建学习任务 / 投递前检查
  -> Agent 执行时展示进度
  -> 结果沉淀回对应产品资产
  -> 工作台刷新项目状态和下一步行动
```

M13 的用户体验目标是：

```text
用户围绕“岗位项目”推进，而不是围绕“工具”和“ID”推进。
```

## 5. 目标页面结构

第一版不做新的复杂页面路由，可以在现有聊天界面上固化一个“项目工作台”视图。

推荐布局：

```text
CareerWorkbenchShell
  ├── CareerProjectHeader
  ├── CareerReadinessPanel
  ├── CareerActionBar
  ├── CareerLinkedAssetsSection
  ├── CareerTimelineSection
  ├── CareerNextActionsPanel
  └── CareerDetailDrawer
```

### 5.1 CareerWorkbenchShell

职责：

- 承载某一个 `CareerApplication` 的完整工作台。
- 管理选中项目、刷新、加载、错误和空状态。
- 不常驻遮挡聊天主区域，不做固定右侧大资料栏。

目标效果：

- 用户点击某个求职项目后，进入一个清晰的项目视图。
- 项目视图可以是弹层、底部抽屉或主内容区域切换，但不能挡住输入区和核心对话。

### 5.2 CareerProjectHeader

展示：

- 公司 / 岗位。
- 阶段：草稿、准备投递、已投递、面试中、暂停等。
- 优先级。
- 最近更新时间。
- 综合匹配度。

交互：

- 可切换阶段和优先级。
- 可手动刷新。
- 不展示裸 ID 作为主信息；ID 只放在调试入口或详情里。

### 5.3 CareerReadinessPanel

展示：

- 投递准备度。
- 核心优势。
- 关键短板。
- 是否建议投递。
- 需要补齐的材料。

数据来源：

- `CareerApplication.summary`
- `CareerApplication.risks`
- `CareerApplication.next_actions`
- `JobFitReport.score`
- `JobFitReport.risks`
- 最新 `ResumeVersion`

目标效果：

- 用户一眼知道“现在能不能投”。
- 不用打开匹配报告全文也能理解当前状态。

### 5.4 CareerActionBar

提供用户能理解的动作，不暴露工具名：

```text
生成定制简历
投递前检查
准备面试
保存准备笔记
创建学习任务
查看匹配报告
```

交互规则：

- 点击动作后，由前端生成稳定 prompt 注入对话。
- prompt 内可以带 `application_id`，但 UI 不把 ID 当主体验。
- 动作执行时，进度面板展示 Agent 正在做什么。
- 动作完成后刷新工作台聚合数据。

### 5.5 CareerLinkedAssetsSection

展示关联资产：

```text
简历画像
职业画像
JD 分析
匹配报告
定制简历版本
笔记
学习计划 / 学习任务
来源文件
```

交互：

- 资产卡片只显示用户能理解的信息：标题、类型、更新时间、状态。
- 支持预览和下载用户可见文件。
- 调试型 ID 放在次级菜单中，不放在主按钮上。
- 多份相同类型资产按“当前使用 / 历史版本”分组，不平铺堆满。

### 5.6 CareerTimelineSection

展示项目推进记录：

```text
创建求职项目
生成匹配报告
生成定制简历
保存面试准备笔记
创建学习任务
完成投递前检查
用户手动更新阶段
```

第一版可以由现有记录的 `created_at / updated_at` 和 session events 组合生成，不新增独立 TimelineStore。

### 5.7 CareerNextActionsPanel

展示：

- 下一步行动。
- 风险处理建议。
- 今日建议。

交互：

- 点击某个行动项可以直接填入输入框或触发动作。
- 如果行动项来自 LearningTask，点击进入学习任务详情。
- 如果行动项来自 CareerApplication，只更新求职项目，不自动写 memory。

### 5.8 CareerDetailDrawer

详情抽屉用于承载较重内容：

- 匹配报告预览。
- 定制简历预览。
- 笔记全文。
- 学习任务详情。
- 原始文件预览。

原则：

- 主界面保持清爽。
- 长文本进入详情抽屉。
- Markdown 必须统一走 Markdown 渲染，不再靠局部解析。

## 6. 后端聚合层

当前前端已经能分别读取 Career、Note、Learning、Knowledge 等接口，但工作台不应该让前端自行拼接大量接口。

M13 第一批建议新增只读聚合服务：

```text
CareerWorkspaceService
```

职责：

- 读取 CareerApplication。
- 聚合关联的 ResumeProfile、CareerProfile、JDAnalysis、JobFitReport、ResumeVersion。
- 查询 related_application_id / target_application_id 关联的 Note、LearningPlan、LearningTask、WeaknessTracker、Knowledge。
- 输出前端可直接渲染的工作台视图。
- 不写入任何 store。
- 不调用模型。
- 不调用 RetrievalService 做语义搜索，第一版只按显式 id 和关联字段聚合。

### 6.1 推荐 API

```text
GET /api/career/workbench
GET /api/career/workbench/applications/{application_id}
```

`GET /api/career/workbench` 用于工作台列表：

```text
CareerWorkbenchListView
  applications: CareerApplicationSummaryView[]
  active_application_id: string | null
  counts
  updated_at
```

`GET /api/career/workbench/applications/{application_id}` 用于单项目详情：

```text
CareerApplicationWorkbenchView
  application
  readiness
  linked_assets
  notes
  learning
  timeline
  suggested_actions
```

### 6.2 关键 ViewModel

建议定义这些 schema：

```text
CareerApplicationSummaryView
CareerApplicationWorkbenchView
CareerReadinessView
CareerLinkedAssetView
CareerTimelineItemView
CareerSuggestedActionView
CareerLearningSummaryView
CareerNoteSummaryView
```

### 6.3 CareerLinkedAssetView

统一资产展示格式：

```text
type
id
title
subtitle
status
updated_at
preview_artifact_id
source_session_id
is_current
actions
```

注意：

- `id` 可以给前端内部使用，但 UI 主视觉不突出裸 ID。
- 文件预览必须使用 `artifact_id`，不能暴露路径。
- `is_current` 用于区分当前简历版本、当前匹配报告和历史版本。

### 6.4 CareerReadinessView

推荐字段：

```text
score
level
recommendation
summary
strengths
risks
missing_materials
next_actions
```

第一版不新建评分算法，用已有数据做规则聚合：

- 优先取 `JobFitReport.overall_score`。
- `recommendation` 来自 `JobFitReport.recommendation`。
- `risks` 合并 `CareerApplication.risks` 和 `JobFitReport.risks`。
- `next_actions` 优先取 `CareerApplication.next_actions`。

## 7. 前端改造范围

M13 第一批前端不做大重构，围绕现有 `career_assets_panel` 演进。

建议新增或拆分组件：

```text
CareerWorkbenchLauncher
CareerWorkbenchSheet
CareerProjectHeader
CareerReadinessPanel
CareerActionBar
CareerLinkedAssetsGrid
CareerTimelineList
CareerNextActionsPanel
CareerAssetPreviewDrawer
```

现有 `CareerApplicationCard` 可以保留，但点击后进入 `CareerWorkbenchSheet`，而不是只展开资产详情。

### 7.1 输入区联动

上传资料后，输入区推荐动作应该基于当前状态：

```text
只有简历：诊断简历 / 生成职业画像
有简历和 JD：分析匹配 / 生成定制简历
有 CareerApplication：投递前检查 / 面试准备 / 创建学习任务 / 保存笔记
```

用户不应该猜提示词。

### 7.2 进度面板联动

Agent 执行动作时，继续使用现有进度面板。

M13 需要保证：

- 工作台动作触发后，进度面板能显示当前阶段。
- 完成后自动刷新工作台数据。
- 失败时给出明确失败阶段和可重试动作。

## 8. 数据边界

M13 必须继续守住这些边界：

- 工作台聚合层只读，不写 memory。
- 工作台聚合层不创建 Note、Learning、Career 记录。
- 用户动作仍通过 Agent 和工具写入。
- 文件事实仍以 SessionArtifact 为准。
- 前端预览只使用 `artifact_id`，不使用路径。
- Note、Learning、Career 之间可以互相引用受控 id，但不要跨 store 做强存在性校验。

## 9. 第一批开发范围

推荐第一批只做后端聚合层和测试：

```text
1. app/career/workbench.py
2. app/schemas/career_workbench.py
3. app/api/career_workbench.py
4. tests/test_career_workbench_service.py
5. tests/test_career_workbench_api.py
6. docs/product_service_roadmap.md 更新
```

第一批验收标准：

- 能列出求职项目工作台摘要。
- 能读取单个求职项目工作台详情。
- 能聚合 CareerApplication 关联的核心 Career 记录。
- 能聚合 Note / Learning 中显式关联当前 application 的记录。
- 能输出 linked_assets、readiness、timeline、suggested_actions。
- 不写入任何 store。
- 不暴露路径。
- 不依赖模型。

## 10. 第二批开发范围

第二批再做前端：

```text
1. 接入 CareerWorkbench API
2. 改造 CareerApplicationCard 点击行为
3. 新增 CareerWorkbenchSheet
4. 新增 CareerReadinessPanel / CareerActionBar / CareerLinkedAssetsGrid
5. 完成预览抽屉统一入口
6. 补充 Flutter widget tests
```

第二批验收标准：

- 用户能从求职项目卡片进入工作台。
- 用户能看懂当前岗位状态、风险和下一步行动。
- 用户能一键触发 M12 已验证动作。
- 用户能预览匹配报告、简历版本和笔记。
- 调试 ID 不作为主视觉。

## 11. 第三批开发范围

第三批做真实链路体验验收：

```text
1. 上传简历 + JD
2. 生成求职项目
3. 打开工作台
4. 触发投递前检查
5. 触发面试准备
6. 保存准备笔记
7. 创建学习任务
8. 工作台刷新并展示新资产
```

验收方式：

- 低批次真实 smoke。
- 前端截图检查。
- 不做压力测试。

## 12. 当前结论

M13 的核心不是“再造一个复杂前端”，而是把产品主线收束到 `CareerApplication`。

后续用户应该围绕一个求职项目持续推进：

```text
这个岗位我现在是什么状态？
我能不能投？
我缺什么？
下一步做什么？
做完后系统有没有记录下来？
```

如果 M13 做稳，产品会从“能聊天完成求职任务”进入“能长期管理求职过程”的阶段。
