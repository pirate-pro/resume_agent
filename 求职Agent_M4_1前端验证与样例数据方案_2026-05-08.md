# 求职 Agent M4.1 前端验证与样例数据方案

日期：2026-05-08

## 1. 目标

M4 已经完成只读求职资产面板的代码接入，但当前环境缺少 `dart` / `flutter` 命令，前端还没有完成真实编译和交互验证。

M4.1 的目标是补齐前端验证闭环需要的两件事：

```text
1. 一套明确的前端验收清单
2. 一套可复现的求职资产样例数据
```

完成后，只要本机具备 Flutter 环境，就可以用固定 demo 数据打开右侧“求职资产”面板，验证五类产品资产和 Markdown artifact 预览是否正常。

## 2. 验证范围

### 2.1 包含

- career 列表接口能返回五类产品记录。
- artifact 正文接口能读取诊断报告、岗位匹配报告、定制简历正文。
- 前端右侧面板能展示：
  - `ResumeProfile`
  - `CareerProfile`
  - `JDAnalysis`
  - `JobFitReport`
  - `ResumeVersion`
- 面板能切换 tab。
- 面板能点击记录查看详情。
- 面板能点击 artifact 预览按钮查看正文。
- 桌面端右侧“求职资产 / 调试面板”切换正常。
- 窄屏端求职资产底部抽屉可打开。

### 2.2 不包含

- 不验证产品记录写入工具。
- 不验证真实 LLM 输出质量。
- M4.1 不做 UI 自动化截图测试，M4.2 已补充构建版截图验收记录。
- 不做性能压测。
- 不做产品记录编辑、归档、删除。

## 3. 样例数据

样例数据固定写入一个 demo session：

```text
sess_m4_frontend_demo
```

固定 artifact：

```text
artifact_m4_resume
artifact_m4_diagnosis
artifact_m4_jd
artifact_m4_fit_report
artifact_m4_resume_version
```

固定产品记录：

```text
resume_profile_m4_demo
career_profile_m4_demo
jd_m4_demo
fit_m4_demo
resume_version_m4_demo
```

这些 id 都是 demo 专用，避免覆盖用户真实的 `career_profile_default` 或真实求职记录。

## 4. 样例数据命令

生成默认 `data/` 下的样例数据：

```bash
uv run python scripts/seed_career_frontend_demo.py --data-dir data
```

生成到临时目录：

```bash
uv run python scripts/seed_career_frontend_demo.py --data-dir /tmp/resume_agent_m4_demo
```

脚本是幂等的。重复运行会更新同一批 demo artifact 和产品记录，不会生成重复 id。

## 5. 后端接口验收

启动后端：

```bash
uv run uvicorn app.main:app --reload --workers 1
```

检查五类产品记录：

```bash
curl http://localhost:8000/api/career/resumes
curl http://localhost:8000/api/career/profiles
curl http://localhost:8000/api/career/jobs
curl http://localhost:8000/api/career/job-fit-reports
curl http://localhost:8000/api/career/resume-versions
```

检查 artifact 正文预览：

```bash
curl "http://localhost:8000/api/sessions/sess_m4_frontend_demo/artifacts/artifact_m4_diagnosis/content?max_chars=12000"
curl "http://localhost:8000/api/sessions/sess_m4_frontend_demo/artifacts/artifact_m4_fit_report/content?max_chars=12000"
curl "http://localhost:8000/api/sessions/sess_m4_frontend_demo/artifacts/artifact_m4_resume_version/content?max_chars=12000"
```

期望：

- response `code` 为 `0`。
- `data.content` 非空。
- 不出现任何本地文件路径字段。

## 6. 前端人工验收

启动前端：

```bash
cd flutter_app
flutter run -d chrome
```

桌面端验收：

- 首屏右侧默认出现“求职资产”面板。
- 顶部工具栏有“求职资产”按钮。
- 顶部工具栏有“调试面板”按钮。
- 点击“调试面板”后右侧切换成调试面板。
- 再点击“求职资产”后右侧切回求职资产面板。
- 求职资产面板顶部数量为：
  - 简历 `1`
  - 画像 `1`
  - JD `1`
  - 匹配 `1`
  - 版本 `1`
- `全部 / 简历 / 画像 / JD / 匹配 / 版本` tab 可切换。
- 点击每张卡片后，下方“记录详情”同步变化。
- 点击“预览诊断”能看到简历诊断 Markdown。
- 点击“预览报告”能看到岗位匹配报告 Markdown。
- 点击“预览简历”能看到定制简历 Markdown。

窄屏验收：

- 将浏览器宽度缩到窄屏。
- 右侧固定面板消失。
- 点击顶部“求职资产”按钮，底部抽屉打开。
- 抽屉内数量、tab、卡片、预览能力与桌面端一致。

## 7. UI 重点观察项

需要重点观察这些问题：

- 中文长字段是否溢出卡片。
- tab 数量多时是否能横向滚动。
- artifact 预览长文是否限制高度并可滚动。
- 空状态是否清楚。
- 后端离线或接口报错时错误态是否可见。
- 窄屏抽屉高度是否够用。
- 右侧面板关闭后，顶部按钮 active 状态是否正确。

## 8. 自动化验证

M4.1 初始自动化验证覆盖后端和样例数据生成：

```bash
uv run pytest
uv run mypy
```

前端需要在具备 Flutter 环境后补跑：

```bash
dart format flutter_app/lib
flutter analyze
```

## 9. M4.2 实际验收记录

日期：2026-05-08

本轮已经补齐 Flutter SDK，并完成构建版前端验收。

Flutter SDK 位置：

```text
/home/ubunt/resume_agent/flutter
```

本地调试启动命令：

```bash
cd /home/ubunt/resume_agent/flutter_app
/home/ubunt/resume_agent/flutter/bin/flutter run -d web-server --web-hostname 0.0.0.0 --web-port 38765
```

如果用服务器 IP 访问前端，默认后端地址会跟随当前页面 host 推导：

```text
http://<server-ip>:38765 -> http://<server-ip>:8000
http://localhost:38765 -> http://localhost:8000
```

这避免了远程浏览器访问 `http://localhost:8000` 时落到用户本机的问题。

构建版验收命令：

```bash
cd /home/ubunt/resume_agent/flutter_app
/home/ubunt/resume_agent/flutter/bin/flutter analyze
/home/ubunt/resume_agent/flutter/bin/flutter build web
cd build/web
python -m http.server 38766 --bind 0.0.0.0
```

实际结果：

- `flutter analyze` 通过，结果为 `No issues found!`。
- `flutter build web` 通过，产物输出到 `flutter_app/build/web`。
- 构建版桌面端页面能加载，初始化接口全部返回 200。
- 桌面端“求职资产”面板会读取五类产品记录。
- 点击“预览诊断”后会请求 artifact 正文接口：

```text
GET /api/sessions/sess_m4_frontend_demo/artifacts/artifact_m4_diagnosis/content?offset=0&max_chars=12000
```

- 窄屏 390px 下点击顶部“求职资产”按钮后，底部抽屉能打开并读取五类产品记录。
- 使用服务器 IP 访问构建版前端时，接口请求会指向同一服务器的 `8000` 端口。
- Playwright 生成过桌面、桌面预览、移动端、服务器 IP 访问四张验收截图，未发现页面异常或请求失败。

已知 warning：

- `file_picker` 在 linux、macos、windows 默认 plugin 声明上有 Flutter warning，这是依赖包声明问题，不影响当前 web 构建。
- `flutter build web` 的 wasm dry run 提示 `file_picker` 使用 `dart:html`，当前产物仍按普通 web 构建通过；后续如果要启用 wasm，需要单独处理。
- `CupertinoIcons` 字体 warning 来自依赖字体未被打包，当前界面未依赖该字体展示关键控件，不阻塞 M4.2。

debug web-server 说明：

- `flutter run -d web-server` 适合人工打开浏览器调试。
- 在当前 headless Playwright 环境下，debug web-server 页面只加载 DDC 调试脚本，没有完整 bootstrap Flutter UI。
- 自动化验收采用 `flutter build web` 后的静态产物，避免把 debug runner 限制误判为产品问题。

## 10. 后续判断

如果 M4.1/M4.2 验证通过，M4 才算真正闭环。

如果验证中发现 UI 问题，下一步优先修体验问题，而不是新增编辑、归档、搜索等能力。
