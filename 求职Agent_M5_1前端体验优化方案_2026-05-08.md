# 求职 Agent M5.1 前端体验优化方案

日期：2026-05-08

## 1. 背景

M5 第一批已经让对话框内出现任务进度面板，但真实使用后暴露出三个体验问题：

```text
1. 只能看到 agent 生命周期，看不到 child-agent 内部执行过程。
2. artifact_id / resume_profile_id 等内部 id 被直接展示，用户不知道能做什么。
3. 生成的长文本排版太素，像原始 Markdown 文档，阅读体感差。
```

前端是产品门面，M5.1 的目标不是继续堆功能，而是把“等待、产物、阅读”三件事做得像产品。

## 2. 总目标

M5.1 完成后，用户看到的是：

```text
任务执行中：知道每个 agent 在做什么、做到了哪一步、是否还在运行。
任务完成后：知道生成了哪些资产，每个资产可以预览、下载、复制 id、激活为上下文。
阅读报告时：看到的是结构化诊断页面，而不是一整块生硬文本。
```

## 3. 不解决的问题

本阶段不做：

- 不引入 LangGraph。
- 不做 `delegate_agents(wait=false)`。
- 不做任务取消、重试、DAG。
- 不做产品记录编辑。
- 不做复杂动画。
- 不把 child-agent 私有原始日志完整暴露给前端。

## 4. 当前问题拆解

### 4.1 只看到一个 agent

如果用户只要求“诊断这份简历”，当前只委派 `resume_agent` 是合理的，因为这个任务只需要简历解析 agent。

但体验问题在于：

- 面板只显示 `resume_agent` 开始和完成。
- 看不到 `resume_agent` 内部步骤，例如读取 artifact、生成诊断报告、保存 ResumeProfile。
- 如果后续用户同时要求“诊断简历 + 分析 JD + 匹配岗位”，面板应该展示 `resume_agent`、`job_agent`，并且展示 main-agent 汇总阶段。

因此 M5.1 需要新增“阶段视角”，不是只按 agent 视角展示。

### 4.2 红框里的 id 像 bug

这些 id 本质上是后端事实源引用：

```text
artifact_bdcf7fc5d141 = 文件事实源 id
resume_profile_zhangming_002 = 简历画像产品记录 id
career_profile_default = 职业画像产品记录 id
```

它们对系统有用，但不应该作为主要用户界面文本。

用户界面应该优先显示：

```text
诊断报告 · Markdown · 约 4.8K 字
简历画像 · 张明 · active
职业画像 · 后端工程师方向
```

内部 id 只放在次级位置，并提供复制按钮。

### 4.3 文本阅读体验差

当前回答像普通 Markdown 直出，问题是：

- 标题、摘要、风险点、建议之间层级不明显。
- 关键结果没有卡片化摘要。
- “已创建的产品记录”混在正文里，既不美观，也不可操作。
- 长段落、列表、分割线让内容像日志，不像产品报告。

M5.1 应该把求职类回答渲染成结构化阅读界面。

## 5. 新整体布局

对话区域内的 assistant 消息结构调整为：

```text
AssistantMessageShell
  RunProgressPanel              执行中/完成态进度
  CareerResultOverview          结果摘要卡片
  CareerReportRenderer          结构化正文
  ProductRecordStrip            已创建产品记录
  ArtifactActionStrip           可预览/下载文件
```

右侧资产面板结构调整为：

```text
CareerAssetsPanel
  CareerAssetsHeader
  CareerAssetsSummaryStrip
  CareerAssetTabBar
  CareerAssetList
  CareerAssetDetailPane
  ArtifactPreviewPanel
```

底部输入区域保持：

```text
InputBar
  ActiveArtifactsTray
  UploadButton
  Composer
```

## 6. 组件清单

### 6.1 AssistantMessageShell

位置：

```text
flutter_app/lib/shared/widgets/chat_bubble.dart
```

职责：

- 统一承载 assistant 的进度、报告正文、产物操作。
- 不直接解析业务内容，只决定显示哪些子组件。
- 根据 `message.sourceKind / answerFormat / renderHint / artifacts / progressEvents` 选择渲染策略。

目标效果：

- 一个 assistant 回复看起来像一个完整结果页面。
- 进度和结果在同一个回复里连续展示，不割裂。

交互：

- streaming 阶段显示进度和流式正文。
- done 后保留完成态进度。
- 用户可以复制整段回答。

### 6.2 RunProgressPanel

位置：

```text
flutter_app/lib/shared/widgets/run_progress_panel.dart
```

职责：

- 展示当前这轮任务的执行状态。
- 支持展开/收起。
- 展示阶段、agent、timeline、产物引用。

目标效果：

```text
求职任务执行中
3 个阶段 · 1 运行中 · 2 已完成

1. 简历解析
   resume_agent · 已完成
   12:35:39 已分配任务
   12:35:41 已读取简历 artifact
   12:36:02 已生成诊断报告
   12:36:43 已保存简历画像

2. 职业画像更新
   agent_main · 已完成

3. 结果整理
   agent_main · 运行中
```

组件内部拆分：

```text
RunProgressHeader
RunStageList
RunStageCard
AgentTaskCard
AgentTaskTimeline
AgentTaskTimelineItem
RunProgressFooter
```

交互：

- 点击 header 收起/展开。
- 点击某个 artifact chip，打开 `ArtifactPreviewDrawer`。
- 点击某个 product record chip，右侧 `CareerAssetsPanel` 自动切换到对应 tab 并选中记录。

### 6.3 RunStageCard

职责：

- 把执行过程从“只有 agent”升级为“阶段 + agent”。
- 阶段可以来自后端事件，也可以由前端根据事件类型推断。

阶段建议：

```text
资料准备
简历解析
JD 分析
岗位匹配
职业画像更新
简历版本生成
结果整理
```

状态：

```text
pending
running
completed
failed
skipped
```

目标效果：

- 用户不需要理解 `resume_agent`、`job_agent` 的技术含义，也能知道任务做到了哪一步。

### 6.4 AgentTaskTimeline

职责：

- 展示每个 agent 内部的关键动作。
- 只展示用户可理解、可审计的动作。

timeline item 类型：

```text
task_assigned        已分配任务
artifact_read        已读取资料
tool_started         正在调用工具
artifact_created     已生成文件
product_saved        已保存产品记录
task_completed       任务完成
task_failed          任务失败
```

禁止展示：

- child-agent 完整 prompt。
- 本地路径。
- 长篇工具结果。
- 原始 JSON。

### 6.5 ProductRecordStrip

位置：

```text
flutter_app/lib/shared/widgets/career_result_blocks.dart
```

职责：

- 替代正文里的“已创建的产品记录：resume_profile_xxx”普通文本。
- 将产品记录渲染成可操作卡片。

展示样式：

```text
已创建产品记录
[简历画像] 张明 · resume_profile_zhangming_002   查看详情 | 复制 ID
[诊断报告] Markdown · artifact_bdcf7fc5d141       预览 | 下载 | 复制 ID
[职业画像] 后端工程师方向 · career_profile_default 查看详情 | 复制 ID
```

交互：

- `查看详情`：右侧资产面板切换并选中对应记录。
- `预览`：打开 artifact 预览抽屉。
- `下载`：调用 artifact 下载接口。
- `复制 ID`：复制内部 id，不复制路径。

### 6.6 ArtifactActionStrip

职责：

- 统一处理所有 artifact 引用。
- 解决“文件名/id 看起来像 bug”的问题。

展示主信息：

```text
诊断报告
Markdown · 4.8 KB · 刚刚生成
```

展示次信息：

```text
artifact_bdcf7fc5d141
```

按钮：

```text
预览
下载
激活为上下文
复制 ID
```

文件名的作用：

```text
1. 作为用户可读标题。
2. 作为下载时的默认文件名。
3. 作为文件类型和内容用途的提示。
4. 不作为后端事实源，不作为 agent 工具参数。
```

后端真正事实源仍是：

```text
artifact_id
```

### 6.7 ArtifactPreviewDrawer

职责：

- 在当前页面内预览 artifact 正文。
- 不跳转、不暴露路径。

布局：

```text
Drawer/Header
  标题：诊断报告
  元信息：Markdown · 4.8 KB · artifact_bdcf...
  操作：下载 / 复制 ID / 关闭

Drawer/Body
  MarkdownRenderer
```

交互：

- 从 `ArtifactActionStrip`、`ProductRecordStrip`、右侧资产卡片都能打开。
- 预览失败时显示错误态和重试按钮。
- 长文内部滚动，不撑爆页面。

### 6.8 CareerReportRenderer

职责：

- 把求职类回答从普通 Markdown 变成结构化报告。
- 针对简历诊断、JD 分析、匹配报告、定制简历分别渲染。

子组件：

```text
CareerReportHeader
DiagnosisSummaryCard
StrengthsGrid
RiskPointList
ImprovementPlan
KeywordSkillMatrix
GeneratedAssetSection
NextActionBar
```

目标效果：

- 首屏先给结论。
- 关键优势和风险点卡片化。
- 建议有优先级。
- 产物引用可操作。

### 6.9 DiagnosisSummaryCard

职责：

- 展示诊断结论摘要。

内容：

```text
候选人：张明
目标方向：后端工程师 / AI 应用工程师
整体判断：技术链路匹配，但项目成果量化还不够
优先级：先补 Agent 项目、RAG 细节、成果指标
```

样式：

- 单个紧凑 summary card。
- 不使用大 hero。
- 适合扫描。

### 6.10 RiskPointList

职责：

- 展示简历风险点。

每条风险包含：

```text
风险标题
影响
建议动作
优先级
```

示例：

```text
项目深度描述不足
影响：AI 应用岗位会怀疑只是调用 API
建议：补 Runtime vs LangChain 选型、工具调用失败恢复、事件审计
优先级：高
```

### 6.11 ImprovementPlan

职责：

- 将“关键改进建议”渲染成任务清单。

字段：

```text
title
why
action
priority
related_section
```

交互：

- 可以复制单条建议。
- 后续可以扩展成“生成改写版本”按钮。

### 6.12 NextActionBar

职责：

- 告诉用户下一步做什么。

按钮：

```text
继续匹配 JD
生成定制简历
预览诊断报告
下载诊断报告
```

交互：

- `继续匹配 JD`：聚焦输入框并提示用户粘贴 JD。
- `生成定制简历`：如果缺少 JD，提示先提供 JD。
- `预览/下载`：操作 artifact。

## 7. 后端协议优化

### 7.1 新增 agent_task_progress

当前已有：

```text
agent_task_group_created
agent_task_started
agent_task_completed
agent_task_failed
agent_task_group_completed
```

M5.1 需要新增：

```text
agent_task_progress
```

payload：

```json
{
  "task_group_id": "task_group_xxx",
  "task_id": "task_xxx",
  "target_agent_id": "resume_agent",
  "stage": "resume_parse",
  "status": "running",
  "event_kind": "artifact_read",
  "title": "读取简历",
  "detail": "已读取测试简历_后端工程师_张明.md",
  "artifact_refs": ["artifact_xxx"],
  "product_refs": [],
  "created_at": "2026-05-08T21:00:00+08:00"
}
```

### 7.2 progress 事件来源

第一版来源：

- `session_read_artifact` 成功 -> `artifact_read`
- `session_create_text_artifact` 成功 -> `artifact_created`
- `career_resume_profile_save` 成功 -> `product_saved`
- `career_profile_merge` 成功 -> `product_saved`
- `career_jd_analysis_save` 成功 -> `product_saved`
- `career_job_fit_report_save` 成功 -> `product_saved`
- `career_resume_version_create` 成功 -> `product_saved`

实现方式：

```text
AgentTaskRuntime
  -> child_run_id 已知
  -> 周期性读取 child-agent event log
  -> AgentTaskProgressProjector 过滤并瘦身
  -> 写入 session orchestration event: agent_task_progress
```

这样不需要把 child-agent 私有日志直接给前端。

### 7.3 artifact 下载接口

当前已有正文预览接口：

```text
GET /api/sessions/{session_id}/artifacts/{artifact_id}/content
```

M5.1 建议新增下载接口：

```text
GET /api/sessions/{session_id}/artifacts/{artifact_id}/download
```

规则：

- 使用 `SessionArtifact.title` 作为下载文件名。
- 如果 title 不存在，退化为 `{artifact_id}.txt`。
- 不暴露 `storage_relpath` 或本地路径。
- 只允许下载当前 session 下的 artifact。

## 8. 前端状态模型

新增模型：

```text
RunProgressView
RunStageView
AgentTaskProgressView
TimelineItemView
ProductReferenceView
ArtifactReferenceView
CareerReportView
```

### 8.1 RunProgressView

字段：

```text
run_id
task_group_id
status
stages
tasks
started_at
updated_at
completed_at
```

职责：

- 从 `agent_task_*` 事件归并得到。
- 只保存当前消息需要展示的数据。

### 8.2 ArtifactReferenceView

字段：

```text
artifact_id
title
kind
media_type
size_bytes
role
source_session_id
```

职责：

- 把 artifact id 变成用户可理解的文件卡片。

### 8.3 ProductReferenceView

字段：

```text
record_type
record_id
title
status
source_session_id
artifact_id?
```

职责：

- 把产品记录 id 变成可点击产品资产。

## 9. 交互逻辑

### 9.1 任务执行中

```text
用户发送消息
  -> ChatProvider 开始 streaming
  -> RunProgressController 开始监听 SSE + 轮询 session events
  -> 收到 agent_task_group_created
  -> 创建 RunProgressPanel
  -> 收到 agent_task_started
  -> 对应 AgentTaskCard 变成运行中
  -> 收到 agent_task_progress
  -> AgentTaskTimeline 增加一条进展
  -> 收到 agent_task_completed
  -> AgentTaskCard 变成已完成
  -> 收到 group_completed
  -> RunProgressPanel 变成完成态
```

### 9.2 点击 artifact

```text
点击预览
  -> ArtifactPreviewDrawer 打开
  -> ApiService.readSessionArtifactContent
  -> MarkdownRenderer 展示正文

点击下载
  -> 浏览器打开 /download 接口
  -> 使用 SessionArtifact.title 下载

点击激活为上下文
  -> ChatProvider.activateArtifact
  -> InputBar ActiveArtifactsTray 高亮

点击复制 ID
  -> Clipboard 写入 artifact_id
```

### 9.3 点击产品记录

```text
点击查看详情
  -> CareerAssetsPanel 打开
  -> 切换到对应 tab
  -> 选中对应记录
  -> 滚动到记录位置
```

### 9.4 右侧资产面板刷新

```text
streaming 结束
  -> CareerAssetsProvider.refresh
  -> 如果有新 record id
  -> 对应卡片做 2 秒高亮
```

## 10. 文本渲染规范

### 10.1 不再裸露这类文本

正文里不应该直接显示：

```text
简历画像：resume_profile_zhangming_002
诊断报告：artifact_bdcf7fc5d141
职业档案：career_profile_default
```

应转换为：

```text
已创建 3 个产品资产
[简历画像] 张明               查看详情 复制 ID
[诊断报告] Markdown            预览 下载 复制 ID
[职业画像] 后端工程师方向      查看详情 复制 ID
```

### 10.2 诊断报告布局

推荐结构：

```text
CareerReportHeader
DiagnosisSummaryCard
StrengthsGrid
RiskPointList
ImprovementPlan
GeneratedAssetSection
NextActionBar
```

### 10.3 标题层级

```text
H1：不在气泡内使用超大字号
H2：16-17px，粗体，带轻分割
H3：14-15px，粗体
正文：14-15px，line-height 1.55-1.7
辅助文字：11-12px
```

### 10.4 卡片使用规则

- 只给独立信息块用卡片。
- 不做卡片套卡片。
- 不用大圆角，半径 8-10px。
- 风险、建议、资产分别独立成块。

### 10.5 调试信息展示规则

`artifact_id`、`resume_profile_id`、`career_profile_id`、`jd_analysis_id`、`job_fit_report_id`、`resume_version_id` 是内部追溯标识，主要服务于开发、测试、客服排障和后端审计，不是普通用户的主要阅读内容。

M5.1 阶段保留复制 ID 能力，但展示规则如下：

- ID 不作为正文主文本裸露。
- ID 可以放在资产卡片的次级元信息中，字号和颜色弱化。
- `复制 ID` 放在次级操作位，不抢占 `预览`、`下载`、`查看详情` 等主操作。
- 正式发行版本可以通过配置隐藏 ID 文本和复制 ID 入口，但不影响内部调试构建。
- 后端接口和工具参数仍然只接受受控 ID，不因为前端弱化展示而改用文件名或本地路径。

## 11. 开发顺序

### 第一批：马上修体感最差的问题

```text
1. ArtifactActionStrip 替换当前 artifact 卡片。
2. artifact_id / product_id 在正文中识别并转换为可操作资产块。
3. 新增 artifact download API。
4. ProductRecordStrip 支持查看详情 / 复制 ID。
5. CareerReportRenderer 对简历诊断做结构化渲染。
```

### 第二批：补真实执行过程

```text
1. 新增 agent_task_progress 事件。
2. AgentTaskProgressProjector 从 child-agent 事件中投影安全进展。
3. RunProgressPanel 增加阶段视角。
4. timeline 展示工具级关键节点。
```

### 第三批：细节打磨

```text
1. 新资产高亮。
2. 预览抽屉统一。
3. 移动端适配。
4. 空状态、失败态、加载态。
5. Playwright 截图验收。
```

## 12. 验收标准

### 12.1 进度面板

- 简历诊断任务中，不只显示“resume_agent 已完成”，还要显示读取资料、生成诊断、保存画像等关键节点。
- 如果任务包含多个 agent，面板同时展示多个 AgentTaskCard。
- main-agent 汇总阶段也能显示。
- 失败时能看到失败 agent 和短错误。

### 12.2 artifact 与产品记录

- 页面不再把 artifact id 当主文本展示。
- 每个 artifact 都能预览。
- 可下载 artifact。
- 可复制 artifact id。
- 可激活 artifact 为上下文。
- 产品记录可跳到右侧资产面板。

### 12.3 文本阅读体验

- 首屏能看到诊断摘要，而不是直接进入长文。
- 风险点和建议有清晰卡片。
- “已创建资产”是可操作区块。
- 长文不会撑爆气泡。
- 移动端不横向溢出。

## 13. 风险和边界

- 如果 LLM 输出格式不稳定，前端不能完全依赖 Markdown 文本解析；应优先使用 `artifacts`、`tool_result`、career API 返回的结构化数据。
- `agent_task_progress` 必须瘦身，不能把 child-agent prompt 或长工具结果泄漏到 session event。
- 下载接口必须走 artifact id，不允许路径参数。
- 产品记录跳转右侧面板需要 provider 间通信，避免在 UI 组件里硬编码全局状态。
