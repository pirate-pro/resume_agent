# 求职 Agent M2 工具层契约与权限方案

日期：2026-05-07

## 1. 目标

M2 的目标是把 M1 已经落地的 `CareerProductStore` 接入受控工具层，让 agent 可以创建、读取和复用求职产品资产。

M2 不是重新提炼产品方向，也不改 prompt。它只定义下一步开发需要遵守的工具契约、权限边界和测试验收标准。

完成 M2 后，系统应该具备这条最小闭环的后端工具能力：

```text
简历 artifact
  -> resume_agent 创建 ResumeProfile 和诊断 artifact
  -> agent_main merge CareerProfile
  -> 粘贴 JD 先创建 SessionArtifact
  -> job_agent 创建 JDAnalysis 和 JobFitReport
  -> agent_main 创建最终 ResumeVersion
```

## 2. M2 范围

### 2.1 包含

- 新增无路径的 text artifact 创建工具，用于粘贴 JD、诊断报告、匹配报告和 markdown 简历版本。
- 新增 `app/tools/builtin_tools/career.py`。
- 注册 career 工具到现有 `ToolRegistry`。
- 按 agent 职责配置 career 工具白名单。
- 新增 `tests/test_career_tools.py`。
- 补充必要的工具注册和能力矩阵测试。

### 2.2 不包含

- 不改 prompt。
- 不改 `AGENT.md`。
- 不做前端。
- 不做 API。
- 不做跨 store 数据迁移。
- 不做跨产品记录引用存在性校验。
- 不新增 typed ref。
- 不创建 `data/career/.../*.md` 内部快照。
- 不让 agent 传入或读取任意文件路径。

## 3. 核心原则

### 3.1 artifact-first

所有原文和用户可见文件继续以 `SessionArtifact` 为事实源。

规则：

- 上传简历已经是 `SessionArtifact`，career 工具只接收 `artifact_id`。
- 用户粘贴 JD 时，先创建 `SessionArtifact(kind=pasted_text)`，再创建 `JDAnalysis`。
- 简历诊断报告、岗位匹配报告、最终简历 markdown 都先创建 `SessionArtifact(kind=generated_file)`。
- career 产品记录只保存结构化字段和 artifact 引用。

### 3.2 工具不暴露路径

career 工具参数只允许受控 id 和结构化 JSON 字段：

```text
artifact_id
resume_profile_id
career_profile_id
jd_analysis_id
job_fit_report_id
resume_version_id
```

禁止：

```text
path
file_path
workspace_path
absolute_path
relative_path
```

### 3.3 store 继续保持独立

`CareerProductStore` 仍然不依赖 session 仓储。跨 store 的检查只能放在工具层。

工具层允许做的检查：

- `artifact_id` 是否属于当前 session。
- 需要读取 artifact 文本时，复用现有 session artifact helper。
- 创建用户可见产物时，先写 session artifact，再把返回的 `artifact_id` 写入 career 记录。

工具层不做的检查：

- 不验证 `evidence_refs` 指向的所有产品记录是否真实存在。
- 不为了保存 career 记录去读取其他 store 的内部文件路径。

### 3.4 时间戳和来源由工具与 store 共同收口

写入工具不接收：

```text
created_at
updated_at
source_session_id
status
```

规则：

- `source_session_id` 由 `RunContext.session_id` 设置。
- `status` 默认 `active`。
- `created_at`、`updated_at` 由 `CareerProductStore` 设置。
- 归档工具不在 M2 暴露，归档先保留给后续 API 或管理能力。

## 4. 先补一个无路径 SessionArtifact 工具

现有 `publish_artifact` 依赖 workspace path。M2 需要一个更适合 agent 的 text artifact 创建工具，避免为了发布报告而暴露路径。

### 4.1 工具名

```text
session_create_text_artifact
```

### 4.2 文件位置

```text
app/tools/builtin_tools/session_artifacts.py
```

### 4.3 参数

```json
{
  "title": "岗位 JD.txt",
  "content": "JD 原文或 markdown 内容",
  "kind": "pasted_text | generated_file",
  "media_type": "text/plain | text/markdown",
  "description": "可选说明"
}
```

### 4.4 规则

- 不接收 path。
- `content` 必须是非空字符串。
- `kind` 只允许 `pasted_text` 或 `generated_file`。
- `media_type` 只允许 `text/plain` 或 `text/markdown`。
- 创建后的 artifact 直接为 `status=ready`。
- `visibility=session_shared`。
- `owner_agent_id` 使用 `RunContext.agent_id`。
- `source_type` 建议使用 `tool_session_create_text_artifact`。
- 返回 JSON 必须包含 `artifact_id`、`title`、`kind`、`media_type`、`text_char_count`。

### 4.5 典型用途

```text
agent_main:
  用户粘贴 JD -> session_create_text_artifact -> artifact_id 传给 job_agent

resume_agent:
  简历诊断 markdown -> session_create_text_artifact -> diagnosis_artifact_id

job_agent:
  岗位匹配报告 markdown -> session_create_text_artifact -> report_artifact_id

agent_main:
  最终简历 markdown -> session_create_text_artifact -> ResumeVersion.artifact_id
```

## 5. Career 工具清单

M2 采用细粒度工具，不做一个万能 `career_record_save`。

### 5.1 ResumeProfile 工具

```text
career_resume_profile_save
career_resume_profile_get
career_resume_profile_list
```

#### `career_resume_profile_save`

用途：

- 由 `resume_agent` 保存结构化简历画像。

参数：

```json
{
  "resume_profile_id": "resume_profile_xxx，可选；不传则工具生成",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["artifact_xxx"],
  "basic_info": {},
  "education": [],
  "work_experience": [],
  "project_experience": [],
  "skills": [],
  "certificates": [],
  "awards": [],
  "self_evaluation": "",
  "raw_text_artifact_id": "artifact_xxx",
  "diagnosis_artifact_id": "artifact_xxx",
  "diagnosis": {}
}
```

规则：

- `source_artifact_id` 必须是当前 session artifact。
- `diagnosis_artifact_id` 如果提供，也必须是当前 session artifact。
- 不接收 `source_session_id`、`created_at`、`updated_at`、`status`。
- 返回完整 `ResumeProfile` payload。

### 5.2 CareerProfile 工具

```text
career_profile_get
career_profile_merge
```

#### `career_profile_get`

参数：

```json
{
  "career_profile_id": "career_profile_default"
}
```

规则：

- `career_profile_id` 可选，默认 `career_profile_default`。
- 只读，不自动创建。

#### `career_profile_merge`

用途：

- 由 `agent_main` 汇总简历画像、JD 分析或用户明确输入后更新职业画像。

参数：

```json
{
  "career_profile_id": "career_profile_default",
  "updates": {
    "career_goal": "",
    "target_roles": [],
    "preferred_industries": [],
    "preferred_cities": [],
    "strengths": [],
    "weaknesses": [],
    "skills": [],
    "interests": [],
    "education_summary": "",
    "experience_summary": "",
    "resume_issues": [],
    "interview_weaknesses": []
  },
  "evidence_refs": ["resume_profile_xxx", "artifact_xxx"],
  "source_artifact_id": "artifact_xxx，可选"
}
```

规则：

- 只支持 merge。
- `evidence_refs` 必填且非空。
- 空字符串不覆盖旧值。
- 列表字段去重追加。
- 不支持删除、回滚、整体覆盖。

### 5.3 JDAnalysis 工具

```text
career_jd_analysis_save
career_jd_analysis_get
career_jd_analysis_list
```

#### `career_jd_analysis_save`

用途：

- 由 `job_agent` 保存一个 JD 的结构化分析。

参数：

```json
{
  "jd_analysis_id": "jd_xxx，可选；不传则工具生成",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["artifact_xxx"],
  "company": "",
  "position": "",
  "seniority": "",
  "required_skills": [],
  "preferred_skills": [],
  "responsibilities": [],
  "keywords": [],
  "risk_signals": [],
  "interview_focus": []
}
```

规则：

- `source_artifact_id` 指 JD artifact。
- `source_artifact_id` 必须是当前 session artifact。
- 不接收 `source_session_id`、`created_at`、`updated_at`、`status`。

### 5.4 JobFitReport 工具

```text
career_job_fit_report_save
career_job_fit_report_get
career_job_fit_report_list
```

#### `career_job_fit_report_save`

用途：

- 由 `job_agent` 保存一次简历画像和 JD 之间的匹配报告。

参数：

```json
{
  "job_fit_report_id": "fit_xxx，可选；不传则工具生成",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": ["resume_profile_xxx", "career_profile_default", "jd_xxx", "artifact_xxx", "artifact_report"],
  "jd_analysis_id": "jd_xxx",
  "resume_profile_id": "resume_profile_xxx",
  "career_profile_id": "career_profile_default",
  "overall_score": 82,
  "score_breakdown": {
    "skills": 80,
    "projects": 85
  },
  "matched_evidence": [],
  "gaps": [],
  "resume_optimization_direction": [],
  "interview_preparation_focus": [],
  "recommendation": "recommended | cautious | not_recommended",
  "report_artifact_id": "artifact_xxx"
}
```

规则：

- `source_artifact_id` 指 JD artifact。
- `report_artifact_id` 指用户可见报告 artifact。
- `source_artifact_id` 和 `report_artifact_id` 都必须是当前 session artifact。
- `job_fit_report_id` 必须保持全局唯一，沿用 store 约束。
- 工具可以读取 `ResumeProfile`、`CareerProfile`、`JDAnalysis` 以辅助校验当前入参，但不校验 `evidence_refs` 的全部存在性。

### 5.5 ResumeVersion 工具

```text
career_resume_version_create
career_resume_version_get
career_resume_version_list
```

#### `career_resume_version_create`

用途：

- 由 `agent_main` 创建最终简历版本产品记录。

参数：

```json
{
  "resume_version_id": "resume_version_xxx，可选；不传则工具生成",
  "base_resume_profile_id": "resume_profile_xxx",
  "target_jd_analysis_id": "jd_xxx，可选",
  "title": "AI 应用开发简历版本",
  "artifact_id": "artifact_xxx",
  "evidence_refs": ["resume_profile_xxx", "jd_xxx", "fit_xxx", "artifact_xxx"],
  "change_summary": [],
  "keyword_strategy": [],
  "risk_notes": []
}
```

规则：

- `format` M2 固定为 `markdown`，不开放给 agent 传其他值。
- `artifact_id` 必须是当前 session artifact。
- `source_artifact_id` 由工具设置为 `artifact_id`。
- 不接收 `source_session_id`、`created_at`、`updated_at`、`status`。

## 6. 工具返回格式

所有 career 工具返回 JSON 字符串，至少包含：

```json
{
  "record_type": "resume_profile",
  "record_id": "resume_profile_xxx",
  "status": "active",
  "source_session_id": "sess_xxx",
  "source_artifact_id": "artifact_xxx",
  "evidence_refs": [],
  "created_at": "2026-05-07T00:00:00Z",
  "updated_at": "2026-05-07T00:00:00Z",
  "record": {}
}
```

列表工具返回：

```json
{
  "record_type": "resume_profile",
  "records": []
}
```

读取不存在的记录：

- 不返回伪造空对象。
- 抛 `ToolExecutionError`，错误文案包含 record id。

## 7. 权限矩阵

M2 要按职责拆工具。为了让权限真实生效，建议把 `agent_main` 当前的 `"*"` 工具白名单改为显式列表。

### 7.1 `agent_main`

允许：

```text
delegate_agents
agent_task_status
session_list_artifacts
session_plan_artifact_access
session_read_artifact
session_search_artifact
session_create_text_artifact
state_set
state_publish
state_list
memory_write
memory_search
memory_inspect
memory_explain
memory_forget
memory_update
workspace_write_file
workspace_read_file
publish_artifact
career_resume_profile_get
career_resume_profile_list
career_profile_get
career_profile_merge
career_jd_analysis_get
career_jd_analysis_list
career_job_fit_report_get
career_job_fit_report_list
career_resume_version_create
career_resume_version_get
career_resume_version_list
```

不允许：

```text
career_resume_profile_save
career_jd_analysis_save
career_job_fit_report_save
```

原因：

- 简历画像由 `resume_agent` 写。
- JD 分析和匹配报告由 `job_agent` 写。
- `agent_main` 负责汇总、merge 职业画像、创建最终简历版本。

### 7.2 `resume_agent`

允许：

```text
session_list_artifacts
session_plan_artifact_access
session_read_artifact
session_search_artifact
session_create_text_artifact
career_resume_profile_save
career_resume_profile_get
```

不允许：

```text
career_profile_merge
career_jd_analysis_save
career_job_fit_report_save
career_resume_version_create
```

### 7.3 `job_agent`

允许：

```text
session_list_artifacts
session_plan_artifact_access
session_read_artifact
session_search_artifact
session_create_text_artifact
career_resume_profile_get
career_profile_get
career_jd_analysis_save
career_jd_analysis_get
career_job_fit_report_save
career_job_fit_report_get
```

不允许：

```text
career_profile_merge
career_resume_profile_save
career_resume_version_create
```

## 8. 推荐开发顺序

```text
1. session_create_text_artifact 工具
2. tests/test_career_tools.py 的基础 fixture
3. app/tools/builtin_tools/career.py 的只读工具
4. ResumeProfile 写入工具
5. CareerProfile merge 工具
6. JDAnalysis 写入工具
7. JobFitReport 写入工具
8. ResumeVersion 创建工具
9. 工具注册导出
10. capability 配置显式白名单
11. 权限和工具 schema 测试
```

顺序理由：

- 先解决 artifact-first 的入口和输出承载问题。
- 再接只读工具，降低 store 接入风险。
- 写入工具按产品流程逐步推进。
- 最后再改 capability，避免工具未注册时权限测试混乱。

## 9. 测试计划

### 9.1 `session_create_text_artifact`

必须覆盖：

- 创建 `pasted_text` artifact。
- 创建 `generated_file` markdown artifact。
- 返回 artifact id 后可以被 `session_read_artifact` 读取。
- 拒绝空 content。
- 拒绝非法 `kind`。
- 拒绝非法 `media_type`。
- 参数 schema 不包含 path。

### 9.2 career 工具写入

必须覆盖：

- `career_resume_profile_save` 使用 `RunContext.session_id` 作为 `source_session_id`。
- 写入工具不接受 `created_at`、`updated_at`、`status`。
- `source_artifact_id` 不属于当前 session 时失败。
- `diagnosis_artifact_id`、`report_artifact_id`、`artifact_id` 不属于当前 session 时失败。
- `CareerProfile` merge 必须提供非空 `evidence_refs`。
- `JobFitReport` 重复 `fit_id` 失败。
- `ResumeVersion` 固定写入 `format=markdown`。

### 9.3 career 工具读取

必须覆盖：

- get 已存在记录返回完整 payload。
- get 不存在记录抛 `ToolExecutionError`。
- list 默认只返回 active。
- list 可选 `include_archived=true`。

### 9.4 权限

必须覆盖：

- `resume_agent` 可以保存 `ResumeProfile`。
- `resume_agent` 不能保存 `JDAnalysis`、`JobFitReport`、`ResumeVersion`。
- `job_agent` 可以读取 `ResumeProfile` 和 `CareerProfile`。
- `job_agent` 可以保存 `JDAnalysis` 和 `JobFitReport`。
- `job_agent` 不能 merge `CareerProfile`。
- `agent_main` 可以 merge `CareerProfile` 和创建 `ResumeVersion`。
- `agent_main` 不能直接调用 child-agent 专属写入工具。

### 9.5 不做的测试

M2 不测试：

- prompt 是否稳定调用这些工具。
- 端到端聊天是否完整跑通。
- 前端展示。
- API 路由。

这些进入后续里程碑。

## 10. 实现文件清单

预计新增或修改：

```text
app/tools/builtin_tools/session_artifacts.py
app/tools/builtin_tools/career.py
app/tools/builtin_tools/__init__.py
app/tools/builtins.py
app/api/dependencies/tools.py
app/config/agent_capabilities.json
app/runtime/agent_capability.py
tests/test_career_tools.py
tests/test_tool_registry.py
```

预计不修改：

```text
app/agents/default/AGENT.md
app/agents/resume_agent/AGENT.md
app/agents/job_agent/AGENT.md
app/prompts/
app/career/models.py
app/career/store.py
```

如果开发中发现 M1 store 还需要小修，只能围绕工具接入暴露的问题做最小修改，不扩展模型范围。

## 11. 验收标准

M2 完成时需要满足：

1. `session_create_text_artifact` 可以无路径创建 ready 状态的 session shared text artifact。
2. career 写入工具不接收 `created_at`、`updated_at`、`source_session_id`、`status`。
3. career 工具只接收受控 id，不接收路径。
4. 粘贴 JD 可以先变成 `SessionArtifact`，再被 `job_agent` 用 `artifact_id` 读取。
5. 用户可见 markdown 输出可以先变成 `SessionArtifact`，再被 career 记录引用。
6. `resume_agent`、`job_agent`、`agent_main` 的工具权限和第 7 节一致。
7. `CareerProductStore` 仍然独立，不引入 session 仓储依赖。
8. `evidence_refs` 仍只做格式校验，不做跨记录存在性校验。
9. `uv run pytest tests/test_career_product_store.py` 通过。
10. `uv run pytest tests/test_career_tools.py` 通过。
11. `uv run mypy` 通过。
12. `uv run pytest` 通过。

## 12. 下一里程碑预告

M2 通过审核并开发完成后，再进入 M3：

```text
AGENT.md 契约调整
确定性工具调用流程测试
最小端到端求职闭环测试
```

M3 才开始要求 agent 在真实对话中稳定调用这些工具。M2 只负责把工具层和权限层打稳。
