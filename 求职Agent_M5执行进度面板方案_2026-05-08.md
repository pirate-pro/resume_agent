# 求职 Agent M5 执行进度面板方案

日期：2026-05-08

## 1. 目标

M5 的目标是解决真实求职任务等待时间长、用户不知道系统在做什么的问题。

当用户触发简历诊断、JD 分析、岗位匹配或定制简历任务后，对话区域内应出现一个可折叠的执行进度面板，展示：

```text
主 agent 正在做什么
哪些 child-agent 被委派
每个 child-agent 的当前状态
每个 child-agent 的关键执行步骤
最终生成了哪些产品记录和 artifact
```

解析完成后，右侧“求职资产”面板继续承担结果查看职责，展示已经沉淀的 `ResumeProfile`、`CareerProfile`、`JDAnalysis`、`JobFitReport`、`ResumeVersion`。

## 2. 当前判断

M5 不需要引入 LangGraph。

原因：

- 当前系统已经有 `AgentTaskStore`，可以作为 child-agent 生命周期事实源。
- 当前系统已经有 `events` 和 SSE，可以承载运行期可视化事件。
- 当前 `delegate_agents(wait=true)` 已经能并发执行 child-agent，只是前端在等待期间缺少可读进度。
- 当前阶段不需要 DAG、checkpoint、resume、retry、人工中断、跨进程任务调度这些 LangGraph 更擅长的能力。
- 如果现在引入 LangGraph，会带来编排事实源迁移风险，容易和 `AgentTaskStore`、`EventRecorder`、`SessionArtifact`、`CareerProductStore` 的边界搅在一起。

LangGraph 后续仍可以作为可选 orchestrator，但不应成为 M5 的前置条件。

## 3. 产品形态

进度面板显示在当前 assistant streaming bubble 内，位置在答案正文上方或正文占位区域。

界面结构：

```text
AssistantBubble
  RunProgressPanel
    RunProgressHeader
    AgentTaskGroupCard
      AgentTaskCard(resume_agent)
        AgentTaskTimeline
      AgentTaskCard(job_agent)
        AgentTaskTimeline
    RunProgressFooter
  AnswerContent
```

桌面端和移动端都使用同一个面板，不另做右侧面板。右侧“求职资产”仍只展示结果资产。

## 4. 组件命名

前端新增或调整的核心组件：

```text
RunProgressPanel
RunProgressHeader
RunProgressCollapseButton
AgentTaskGroupCard
AgentTaskCard
AgentTaskStatusBadge
AgentTaskTimeline
AgentTaskTimelineItem
AgentTaskArtifactLinks
RunProgressFooter
```

模型和 provider：

```text
RunProgressView
AgentTaskProgressView
AgentTaskTimelineItemView
RunProgressProvider / ChatProvider 内部状态
```

初期可以直接放在 `chat_provider.dart` 和 `chat_bubble.dart`，如果组件变大，再拆到：

```text
flutter_app/lib/features/chat/run_progress_panel.dart
```

## 5. 视觉和交互

### 5.1 默认展开

任务开始后默认展开，用户可以手动收起。

收起态显示：

```text
正在处理求职任务 · 2 个 agent · 1 运行中 · 1 等待中
```

展开态显示：

```text
求职任务执行中
resume_agent    运行中    解析简历 artifact
  20:11:03 已分配任务
  20:11:05 正在读取简历
  20:11:12 已生成诊断 artifact

job_agent       等待中    等待 JD artifact
```

### 5.2 动态效果

允许的动态效果：

- 运行中的 agent 卡片有轻微进度条 shimmer。
- 当前 timeline item 左侧圆点轻微 pulse。
- 状态切换使用短动画。
- 新增 timeline item 时淡入。

不做复杂动画，不影响信息密度和可读性。

### 5.3 状态

任务组状态：

```text
pending
running
partial_failed
completed
failed
```

单个 agent 状态：

```text
pending
running
completed
failed
archived
```

前端展示文案：

```text
pending -> 等待中
running -> 运行中
completed -> 已完成
failed -> 失败
partial_failed -> 部分失败
archived -> 已归档
```

## 6. 后端事件协议

M5 不直接暴露 child-agent 私有 event log，也不让前端读取 agent workspace 路径。

后端只向 session 级别投影受控进度事件：

```text
agent_task_group_created
agent_task_started
agent_task_progress
agent_task_completed
agent_task_failed
agent_task_group_completed
```

SSE event 仍可使用现有 `run_event` 包装，`type` 使用上述事件类型。

建议 payload：

```json
{
  "task_group_id": "task_group_xxx",
  "task_id": "task_xxx",
  "target_agent_id": "resume_agent",
  "status": "running",
  "title": "解析简历",
  "detail": "正在读取简历 artifact",
  "artifact_refs": ["artifact_xxx"],
  "product_refs": ["resume_profile_xxx"],
  "created_at": "2026-05-08T20:00:00+08:00"
}
```

其中：

- `detail` 是短文本，只用于 UI 展示。
- `artifact_refs` 只放 artifact id。
- `product_refs` 只放产品记录 id。
- 不放文件路径。
- 不放 child-agent 完整 prompt。
- 不放长篇工具结果。

## 7. 实现策略

### 7.1 第一批：可等待的进度

第一批只做 task 生命周期可视化：

```text
agent_task_group_created
agent_task_started
agent_task_completed
agent_task_failed
agent_task_group_completed
```

效果：

- 用户能看到 agent 已经被委派。
- 用户能看到哪个 agent 正在跑。
- 用户能看到哪个 agent 成功或失败。
- 用户能看到耗时和最终引用。

这一批不追求 child-agent 内部每个工具调用都实时出现。

### 7.2 第二批：详细进展投影

第二批再补 `agent_task_progress`。

来源可以是：

- child-agent 的受控工具调用事件。
- `career_*_save` 成功后的产品记录 id。
- `session_create_text_artifact` 成功后的 artifact id。
- child-agent 最终 summary。

这一步需要做事件瘦身，避免把 child-agent 私有原始事件完整暴露给前端。

## 8. 为什么不先上 LangGraph

当前缺口是“进度可视化”和“事件投影”，不是“编排能力缺失”。

LangGraph 能解决的问题主要是：

- 复杂 DAG。
- 条件分支。
- checkpoint/resume。
- 节点级 retry。
- 人工介入。
- 长任务后台化。

M5 要解决的是：

```text
已有任务在执行，但用户看不到进展
```

所以应优先增强现有事件链路，而不是替换编排框架。

## 9. 与现有求职资产面板的关系

两者职责不同：

```text
RunProgressPanel = 当前这一轮任务正在怎么执行
CareerAssetsPanel = 已经沉淀了哪些求职产品资产
```

执行中：

- 对话框内展示 `RunProgressPanel`。
- 右侧资产面板可以保持不变。

执行完成：

- `RunProgressPanel` 显示完成态，可收起。
- `CareerAssetsPanel` 刷新后展示新产品记录。

## 10. M5 不包含

- 不引入 LangGraph。
- 不实现 `delegate_agents(wait=false)`。
- 不做任务取消。
- 不做任务重试。
- 不做 DAG 依赖。
- 不做后台任务队列。
- 不暴露 child-agent 私有事件原文。
- 不做产品记录编辑。

## 11. 开发顺序

推荐顺序：

```text
1. 后端定义 agent task 进度事件投影格式。
2. AgentTaskRuntime 在任务组创建、任务开始、任务完成、任务失败时写入 session orchestration event。
3. chat_stream 将这些事件进入当前 SSE 流，或者前端在 streaming 期间轮询 session events。
4. ChatProvider 将 agent_task_* 事件归并成 RunProgressView。
5. ChatBubble/StreamingBubble 渲染 RunProgressPanel。
6. 完成后触发 CareerAssetsProvider refresh。
7. 补测试：事件 payload、状态归并、前端模型解析。
```

如果第 3 步发现当前 tool runner 不方便把子任务事件实时送入 SSE，第一版允许使用 streaming 期间轮询 `/api/sessions/{session_id}/events`，后续再把事件桥接进 SSE。

## 12. 验收标准

后端验收：

- `delegate_agents` 运行时能产生 task group 和 task 生命周期事件。
- task 失败时仍能产生失败事件。
- 事件 payload 不包含本地路径。
- 事件 payload 不包含 child-agent prompt 原文。
- 不破坏现有 `AgentTaskStore` 状态。

前端验收：

- 发起求职任务后，streaming bubble 内出现进度面板。
- 面板可以展开/收起。
- 每个 agent 显示状态、标题、更新时间。
- running 状态有轻量动态效果。
- completed 状态显示生成的 artifact/product id。
- failed 状态显示短错误。
- 最终答案出来后，进度面板仍保留在本轮 assistant 消息中。
- 任务完成后右侧求职资产面板能刷新看到结果。

## 13. 后续升级

后续如果出现这些需求，再评估 LangGraph：

- 用户希望暂停/恢复某个长任务。
- 用户希望任务失败后只重跑某个节点。
- 求职流程变成复杂 DAG。
- 多轮任务需要跨进程后台执行。
- 需要人工审核某个中间节点后再继续。

到那时 LangGraph 可以作为 `AgentOrchestrator` 的一个实现，但仍应复用：

```text
AgentTaskStore
SessionArtifact
CareerProductStore
EventRecorder
MemoryStore
```

