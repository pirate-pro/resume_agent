name: resume_agent_app
description: Agent Runtime Chat Client
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  flutter_riverpod: ^2.6.1
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  file_picker: ^6.2.1
  uuid: ^4.5.2
  gpt_markdown: ^1.1.6
  flutter_math_fork: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
