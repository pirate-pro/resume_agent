# 求职 Agent M4 前端求职资产面板方案

日期：2026-05-08

## 1. 目标

M4 的目标是把 M1/M2/M3 已经落地的求职产品资产从“后端可查”推进到“用户可见、可浏览、可预览”。

本阶段不改变 agent 行为，不新增产品记录写入能力，只做一个只读前端入口：

```text
用户在聊天主界面右侧看到“求职资产”面板
  -> 能查看简历画像、职业画像、JD 分析、匹配报告、简历版本
  -> 能看到记录来源、状态、更新时间和关键字段
  -> 能通过 artifact_id 预览用户可见 Markdown 资产
```

M4 完成后，用户不需要翻日志或打开数据目录，就可以确认一次求职流程到底沉淀了哪些产品资产。

## 2. 当前基础

后端当前已经具备：

- `CareerProductStore`：求职产品资产事实源。
- `SessionArtifact`：文件事实源。
- `GET /api/career/resumes`
- `GET /api/career/profile`
- `GET /api/career/profiles`
- `GET /api/career/jobs`
- `GET /api/career/job-fit-reports`
- `GET /api/career/resume-versions`

前端当前已经具备：

- `HomeScreen`：应用主布局，左侧会话栏、中间聊天区、右侧调试面板。
- `ChatScreen`：聊天主区域和顶部工具栏。
- `_DebugPanel`：右侧调试面板。
- `ApiService`：统一后端请求入口。
- `chatProvider`：聊天、会话、artifact、调试数据状态。

当前缺口：

- 前端没有 career 模型。
- 前端没有 career API 调用。
- 前端没有 career 独立状态管理。
- 前端没有求职资产浏览面板。
- 后端缺少一个按 `artifact_id` 读取 artifact 正文的只读接口，导致前端无法安全预览诊断报告、匹配报告、简历版本正文。

## 3. 最终效果

### 3.1 桌面端效果

桌面端主布局保持三栏：

```text
┌────────────────┬──────────────────────────────┬─────────────────────┐
│ 会话栏          │ 聊天主区域                    │ 求职资产 / 调试面板  │
│ SessionSidebar │ ChatScreen                    │ RightSidePanel       │
└────────────────┴──────────────────────────────┴─────────────────────┘
```

右侧面板默认展示“求职资产”。用户可以通过顶部工具栏按钮在“求职资产”和“调试面板”之间切换。

求职资产面板视觉结构：

```text
┌──────────────────────────────┐
│ 求职资产                 刷新 │
├──────────────────────────────┤
│ 简历 1  职业画像 1  JD 2 ... │
├──────────────────────────────┤
│ 全部 | 简历 | JD | 匹配 | 版本 │
├──────────────────────────────┤
│ ResumeProfileCard            │
│ JDAnalysisCard                │
│ JobFitReportCard              │
│ ResumeVersionCard             │
├──────────────────────────────┤
│ 选中记录详情 / Markdown 预览  │
└──────────────────────────────┘
```

### 3.2 窄屏效果

窄屏不强行保留三栏。点击顶部“求职资产”按钮后，用底部抽屉展示 `CareerAssetsPanel`。

```text
ChatScreen
  -> 点击求职资产按钮
  -> showModalBottomSheet
  -> CareerAssetsPanel(compact: true)
```

### 3.3 用户感知结果

用户应该能快速回答这些问题：

- 现在系统里有几份简历画像？
- 当前职业画像包含哪些目标岗位、城市、优势、短板？
- 某个 JD 分析的公司、岗位、关键词是什么？
- 某份匹配报告的分数和主要差距是什么？
- 最终生成了哪几个简历版本？
- 简历诊断、岗位匹配报告、简历版本正文能否直接预览？

## 4. 设计边界

### 4.1 本阶段包含

- 新增只读求职资产面板。
- 新增前端 career 数据模型。
- 新增前端 career API 方法。
- 新增独立 `careerAssetsProvider`。
- 接入现有 career 查询 API。
- 补一个后端 artifact 正文预览 API。
- 面板支持加载、空状态、错误状态、刷新、记录选择、artifact 正文预览。

### 4.2 本阶段不包含

- 不新增产品记录写入接口。
- 不做编辑、删除、归档。
- 不做复杂筛选和搜索。
- 不改 agent prompt。
- 不改工具权限。
- 不改 `CareerProductStore` 数据结构。
- 不把 career 数据写入 memory。
- 不在前端暴露文件路径。
- 不读取 `data/career/...` 路径。
- 不做移动端完整独立页面。

## 5. 整体布局结构

### 5.1 `HomeScreen`

职责：

- 继续负责应用主布局。
- 增加右侧面板模式。
- 在桌面端展示右侧固定面板。
- 在窄屏端打开底部抽屉。

新增状态建议：

```text
_RightPanelMode.career
_RightPanelMode.debug

bool _rightPanelOpen
_RightPanelMode _rightPanelMode
```

桌面端显示规则：

```text
showDesktopRightPanel = _rightPanelOpen && !isCompact

if _rightPanelMode == career:
  CareerAssetsPanel

if _rightPanelMode == debug:
  _DebugPanel
```

窄屏端显示规则：

```text
点击求职资产按钮 -> _openCompactCareerAssetsPanel
点击调试按钮 -> _openCompactDebugPanel
```

### 5.2 `ChatScreen`

职责：

- 继续负责聊天主区域。
- 顶部工具栏增加“求职资产”入口。
- 不直接持有 career 数据状态。

建议新增属性：

```text
showCareerAssetsToggle
isCareerAssetsPanelOpen
onCareerAssetsToggle
```

顶部按钮建议：

```text
Icons.work_outline_rounded：求职资产
Icons.developer_board_rounded / Icons.tune_rounded：调试面板
```

### 5.3 `RightSidePanel`

M4 可以不单独抽组件，先由 `HomeScreen` 内部控制。

如果后续右侧面板继续扩展，再抽出：

```text
flutter_app/lib/features/home/right_side_panel.dart
```

建议职责：

- 根据 mode 渲染 `CareerAssetsPanel` 或 `_DebugPanel`。
- 控制固定宽度、间距、关闭动作。
- 不承载具体业务逻辑。

## 6. 组件命名

### 6.1 文件结构

建议新增：

```text
flutter_app/lib/core/providers/career_assets_provider.dart
flutter_app/lib/features/career/career_assets_panel.dart
```

建议扩展：

```text
flutter_app/lib/core/models/api_models.dart
flutter_app/lib/core/services/api_service.dart
flutter_app/lib/features/home/home_screen.dart
flutter_app/lib/features/chat/chat_screen.dart
```

### 6.2 组件清单

| 组件名 | 文件 | 职责 |
| --- | --- | --- |
| `CareerAssetsPanel` | `features/career/career_assets_panel.dart` | 求职资产面板根组件 |
| `CareerAssetsHeader` | 同上 | 标题、刷新、关闭 |
| `CareerAssetsSummaryStrip` | 同上 | 展示五类资产数量 |
| `CareerAssetTabBar` | 同上 | 切换全部、简历、职业画像、JD、匹配、版本 |
| `CareerAssetList` | 同上 | 根据当前 tab 展示记录列表 |
| `ResumeProfileCard` | 同上 | 简历画像摘要卡片 |
| `CareerProfileCard` | 同上 | 职业画像摘要卡片 |
| `JDAnalysisCard` | 同上 | JD 分析摘要卡片 |
| `JobFitReportCard` | 同上 | 匹配报告摘要卡片 |
| `ResumeVersionCard` | 同上 | 简历版本摘要卡片 |
| `CareerAssetDetailPane` | 同上 | 选中记录详情 |
| `ArtifactPreviewPanel` | 同上 | 预览 artifact 正文 |
| `CareerAssetEmptyState` | 同上 | 空状态 |
| `CareerAssetErrorState` | 同上 | 错误状态 |
| `CareerAssetLoadingState` | 同上 | 加载状态 |

### 6.3 状态命名

前端 provider：

```text
careerAssetsProvider
CareerAssetsProvider
CareerAssetsTab
CareerAssetSelection
CareerArtifactPreviewState
```

建议 tab：

```text
all
resumes
profiles
jobs
fitReports
versions
```

建议选择态：

```text
recordType
recordId
sourceSessionId
previewArtifactId
```

## 7. 数据模型

### 7.1 前端模型

建议在 `api_models.dart` 增加：

```text
CareerRecordMetaView
ResumeProfileView
CareerProfileView
JDAnalysisView
JobFitReportView
ResumeVersionView
SessionArtifactContentView
```

字段名和后端 response 保持一一映射。

时间字段：

- 使用 `DateTime.tryParse` 解析。
- UI 展示时使用 `DateFormat('MM-dd HH:mm')`。
- 后端已经统一为上海时区，前端只负责展示，不再自行改写时间含义。

### 7.2 Provider 状态

`CareerAssetsProvider` 建议包含：

```text
bool isLoading
bool isRefreshing
String? error

List<ResumeProfileView> resumeProfiles
List<CareerProfileView> careerProfiles
List<JDAnalysisView> jdAnalyses
List<JobFitReportView> jobFitReports
List<ResumeVersionView> resumeVersions

CareerAssetsTab activeTab
CareerAssetSelection? selection

bool isPreviewLoading
String? previewError
SessionArtifactContentView? preview
```

### 7.3 数据刷新

刷新时并发请求：

```text
GET /api/career/resumes
GET /api/career/profiles
GET /api/career/jobs
GET /api/career/job-fit-reports
GET /api/career/resume-versions
```

`GET /api/career/profile` 可以不作为首屏必需接口，因为 `/profiles` 已经能列出职业画像；这样没有默认职业画像时不会让整个面板失败。

## 8. 后端补充接口

为了让前端预览 artifact 正文，需要新增只读接口：

```text
GET /api/sessions/{session_id}/artifacts/{artifact_id}/content
```

Query：

```text
offset: int = 0
max_chars: int = 12000
```

响应：

```text
{
  "session_id": "sess_xxx",
  "artifact_id": "artifact_xxx",
  "title": "岗位匹配报告.md",
  "media_type": "text/markdown",
  "status": "ready",
  "total_chars": 3200,
  "offset": 0,
  "returned_chars": 3200,
  "truncated": false,
  "content": "# 岗位匹配报告..."
}
```

边界：

- 只允许通过 `session_id + artifact_id` 读取。
- 不接受路径参数。
- 不返回 `storage_relpath` 或 `text_relpath`。
- artifact 不存在时返回 404。
- artifact 未解析完成或无正文时返回标准错误。
- 不做跨 session 搜索。

## 9. 面板内容规则

### 9.1 `ResumeProfileCard`

展示：

- `resume_profile_id`
- 状态
- 更新时间
- 姓名、邮箱、电话等 `basic_info` 摘要
- 技能数量、项目数量、工作经历数量
- `diagnosis_artifact_id` 预览入口

### 9.2 `CareerProfileCard`

展示：

- `career_profile_id`
- 职业目标
- 目标岗位
- 目标城市
- 优势、短板
- 更新时间

### 9.3 `JDAnalysisCard`

展示：

- `jd_analysis_id`
- 公司
- 岗位
- 职级
- 必备技能
- 关键词
- 风险信号
- `source_artifact_id` 预览入口

### 9.4 `JobFitReportCard`

展示：

- `job_fit_report_id`
- 总分
- 推荐态度
- 关联 `jd_analysis_id`
- 关联 `resume_profile_id`
- 主要差距
- 优化方向
- `report_artifact_id` 预览入口

说明：

- `source_artifact_id` 指 JD 原文 artifact。
- `report_artifact_id` 指用户可查看的匹配报告 artifact。

### 9.5 `ResumeVersionCard`

展示：

- `resume_version_id`
- 标题
- 关联简历画像
- 关联 JD
- 变更摘要
- 关键词策略
- `artifact_id` 预览入口

## 10. 交互规则

### 10.1 首次打开

- `CareerAssetsPanel` 首次挂载时自动刷新。
- 如果全部列表为空，展示 `CareerAssetEmptyState`。
- 如果部分接口失败，M4 简化处理为整体错误，提示用户刷新。

### 10.2 切换 tab

- 切换 tab 不重新请求后端。
- 切换 tab 时保留已加载数据。
- 如果当前选中记录不属于新 tab，可以清空选择。

### 10.3 点击记录

- 点击卡片后在详情区展示完整字段摘要。
- 详情区不展示完整 JSON，优先展示用户能理解的字段。
- 需要排查时可以在后续版本增加“复制 JSON”，M4 不做。

### 10.4 预览 artifact

- 点击 artifact 预览按钮后调用 artifact 正文接口。
- 预览内容显示在 `ArtifactPreviewPanel`。
- 长文只取前 `12000` 字符，并展示是否截断。
- M4 不做分页加载。

## 11. 验收标准

后端：

- 新增 artifact 正文读取接口。
- 接口通过 `session_id + artifact_id` 读取，不暴露路径。
- 新增接口测试覆盖成功、artifact 不存在、无正文三类情况。

前端：

- 页面能编译通过。
- 右侧可以切换“求职资产”和“调试面板”。
- 求职资产面板能展示五类产品记录数量。
- 求职资产面板能展示每类记录卡片。
- 面板能预览 `diagnosis_artifact_id`、`report_artifact_id`、`artifact_id` 对应正文。
- 空状态、加载状态、错误状态可见。
- 窄屏下求职资产以底部抽屉展示。

质量：

- 不读取本地数据目录。
- 不暴露 artifact 文件路径。
- 不新增写入能力。
- 不影响现有聊天和调试面板。

## 12. 开发顺序

审核通过后按以下顺序开发：

```text
1. 后端补 artifact 正文只读接口
2. 后端补接口测试
3. 前端补 career 数据模型
4. 前端补 ApiService career 方法
5. 前端新增 careerAssetsProvider
6. 前端新增 CareerAssetsPanel
7. HomeScreen / ChatScreen 接入右侧面板切换
8. Flutter format / analyze
9. 后端 pytest / mypy
```

建议第一批提交：

```text
接口：新增 artifact 正文预览接口
```

建议第二批提交：

```text
前端：新增求职资产只读面板
```

## 13. 暂不处理的问题

- 产品记录编辑。
- 产品记录归档。
- 简历版本下载。
- artifact 全文分页。
- career 数据搜索。
- 多用户隔离。
- 权限系统。
- 记录对比。
- 可视化评分图表。

这些能力应该等只读面板稳定后再进入后续里程碑。

## 14. 结论

M4 是求职产品闭环的前端展示层，不改变后端事实源划分：

```text
SessionArtifact = 文件事实源
CareerProductStore = 求职产品资产事实源
AgentTask = 委派执行事实源
events = 审计事实源
memory = 上下文材料
```

本阶段只做“用户能看见已有资产”，不做“用户能修改资产”。这样范围足够小，也能直接暴露真实链路里生成资产的质量问题。
