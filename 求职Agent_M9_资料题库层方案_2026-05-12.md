# 求职 Agent M9 资料与题库层方案

日期：2026-05-12

## 1. M9 一句话目标

建立 `KnowledgeService`，把外部面经、面试题、资料链接、课程资料和公司岗位要求沉淀成可追溯、可复用的产品资产。

M9 不做聊天 Agent 写入工具，不做 RAG，不做 MCP，不做学习计划，不做自动爬虫，不把资料自动写入 memory。先把资料与题库的事实源边界做稳。

```text
后台导入 / 后续知识库清洗 / 未来 MCP 或 RAG 管线
  -> SessionArtifact
  -> KnowledgeService
  -> ExternalResource / ExperiencePost / InterviewQuestion / CompanyProfile / SkillRequirement
  -> 后续 LearningService / RAG / 面试准备可复用
```

## 2. 为什么现在做资料与题库层

M7 已经有 `CareerApplication`，可以围绕一个目标岗位组织 JD、匹配报告、定制简历和下一步动作。

M8 已经有 `NoteService`，可以保存用户自己的笔记、复盘和阶段性理解。

但产品要成为长期求职成长工具，还缺少一个“外部资料和题库资产层”：

- 学生和转行用户需要长期积累目标公司、岗位要求、面经和面试题。
- 面试准备不应该只靠一次聊天临时生成，应该能复用过去沉淀的资料。
- 后续学习计划需要知道“目标岗位需要什么能力”和“用户还缺什么能力”。
- 后续 RAG 需要稳定的产品记录作为来源，而不是直接索引散乱聊天内容。

所以 M9 的目标不是做搜索引擎，而是先建立外部资料和题库的事实源。

## 3. 核心边界

### 3.1 KnowledgeService 不是 NoteService

`KnowledgeService` 保存外部资料和题库资产，例如面经、面试题、公司要求、课程链接。

`NoteService` 保存用户自己的笔记和理解，例如面试复盘、学习总结、回答草稿。

规则：

- 外部资料、公开面经、公共题目默认进入 `KnowledgeService`。
- 用户说“这是我的笔记 / 帮我记到笔记 / 保存我的复盘”时，进入 `NoteService`。
- 用户自己的面试经历、被问到的问题、回答感受和复盘，默认进入 `NoteService`。
- Note 可以引用 Knowledge 记录，但 Note 不替代资料事实源。
- Knowledge 记录可以关联 Note，但不复制 Note 正文。

### 3.2 KnowledgeService 不是 CareerService

`CareerService` 管理用户自己的求职资产和求职项目。

`KnowledgeService` 管理外部资料和题库资产。

规则：

- JD 分析、匹配报告、定制简历仍属于 `CareerService`。
- 公司公开岗位要求、面经总结、面试题库属于 `KnowledgeService`。
- 一个 `CareerApplication` 可以关联多个 Knowledge 记录，用于面试准备和学习计划。
- 不把所有资料塞进 `CareerApplication`，避免求职项目变成杂物箱。

### 3.3 KnowledgeService 不是 SessionArtifact

`SessionArtifact` 是文件事实源，负责保存上传文件、粘贴文本、生成报告和可下载文件。

`KnowledgeService` 保存结构化字段和 artifact 引用。

规则：

- 后台导入、清洗或未来抓取到的长文本，必须先创建 `SessionArtifact`。
- 上传的 PDF、Markdown、截图识别文本等，如果要进入 KnowledgeService，也先作为 artifact 存在。
- 外部链接第一阶段只保存 URL 元数据；如果导入正文，正文保存为 artifact。
- 未来如果做链接抓取，抓取快照也必须先进入 `SessionArtifact`，再抽取 Knowledge 记录。
- KnowledgeService 不创建 `data/knowledge/*.md` Markdown 双事实源。

### 3.4 KnowledgeService 不是 MemoryService

Knowledge 记录是用户可见、可追溯、可管理的产品资产。

Memory 是运行时上下文材料。

规则：

- 保存资料、面经、题目不等于 `memory_write`。
- KnowledgeService 不主动写 memory。
- 用户明确表达长期偏好、稳定目标、长期事实时，仍按现有 memory 规则处理。
- 后续可以通过 RAG 从 Knowledge 召回上下文，但 RAG 不是 M9 范围。

### 3.5 KnowledgeService 暂不做 RAG / MCP

M9 只做稳定记录，不做索引和召回。

后续 M11 再做：

```text
Knowledge / Note / CareerApplication / LearningTask
  -> RAG index
  -> 上下文召回
  -> 可追溯引用
```

MCP 可以作为后续 Knowledge/RAG 能力的对外接口，但不能在产品记录没稳之前提前做。

## 4. M9 范围

### 4.1 做

- `app/knowledge/` 领域层。
- ExternalResource / ExperiencePost / InterviewQuestion / CompanyProfile / SkillRequirement 模型。
- 文件型 JSON store。
- ID 前缀、status、来源字段、时间戳校验。
- `evidence_refs` 字符串格式校验，不做跨 store 存在性校验。
- 原子写入。
- get / list / archive。
- create / update。
- 损坏 JSON 稳定失败。
- API 契约设计。
- agent 边界说明：聊天 Agent 暂不写 Knowledge；用户个人面经和复盘写 Note；Knowledge 后续面向导入、RAG、MCP 和后台管理。

### 4.2 不做

- 自动爬虫。
- 联网搜索。
- RAG / 向量索引。
- MCP。
- 学习计划生成。
- 面试模拟评分。
- 聊天 Agent 写入工具。
- 前端资料库页面。
- Markdown 快照。
- 跨 store 强存在性校验。
- 版权内容再分发。
- 数据迁移。

## 5. 产品对象

### 5.1 ExternalResource

外部资料的统一入口。

建议字段：

```text
resource_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

title
resource_type
url
provider
company
position
target_roles
skill_tags
summary
key_points
raw_artifact_id
related_application_ids
related_note_ids
```

字段说明：

- `resource_id`：`resource_` 前缀。
- `status`：`active | archived`。
- `source_session_id`：来源会话，`sess_` 前缀。
- `source_artifact_id`：输入主来源 artifact，可为空。
- `evidence_refs`：轻量字符串引用，只做格式校验。
- `resource_type`：`link | pasted_text | uploaded_file | article | course | video | repo | book | other`。
- `url`：外部链接，可为空。M9 不抓取链接内容。
- `provider`：来源站点或资料提供方，例如牛客、力扣、GitHub、B 站。
- `company`：关联公司名，可为空。
- `position`：关联岗位名，可为空。
- `target_roles`：适用岗位方向。
- `skill_tags`：能力标签，例如 Python、RAG、系统设计。
- `summary`：资料摘要。
- `key_points`：关键结论。
- `raw_artifact_id`：原文 artifact，通常等于或补充 `source_artifact_id`。
- `related_application_ids`：关联求职项目 id，不做存在性校验。
- `related_note_ids`：关联笔记 id，不做存在性校验。

### 5.2 ExperiencePost

面经记录，用于保存某家公司某个岗位的面试过程。

建议字段：

```text
experience_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

source_resource_id
company
position
seniority
interview_rounds
interview_process
questions
outcome
difficulty
summary
tags
related_application_ids
related_note_ids
```

字段说明：

- `experience_id`：`experience_` 前缀。
- `source_resource_id`：来源资料 id，可为空。
- `company` / `position`：面经对应公司和岗位。
- `seniority`：应届、实习、初级、中级、高级等。
- `interview_rounds`：轮次结构化描述。
- `interview_process`：流程说明。
- `questions`：题目 id 或题目摘要，M9 第一版可以先用字符串列表。
- `outcome`：结果，例如未知、通过、挂掉、等待。
- `difficulty`：`unknown | easy | medium | hard`。
- `summary`：面经摘要。
- `tags`：简单标签。

### 5.3 InterviewQuestion

面试题记录，用于题库沉淀。

建议字段：

```text
question_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

question_text
question_type
difficulty
skill_tags
company
position
source_resource_id
source_experience_id
answer_outline
evaluation_points
common_pitfalls
related_application_ids
related_note_ids
```

字段说明：

- `question_id`：`question_` 前缀。
- `question_type`：`behavioral | technical | system_design | coding | project_deep_dive | hr | company_specific | other`。
- `difficulty`：`unknown | easy | medium | hard`。
- `skill_tags`：题目涉及能力。
- `company` / `position`：公司和岗位，可为空。
- `source_resource_id`：来自哪份资料。
- `source_experience_id`：来自哪条面经。
- `answer_outline`：回答提纲，不要求完整答案。
- `evaluation_points`：面试官可能考察什么。
- `common_pitfalls`：常见答错点。

### 5.4 CompanyProfile

公司求职画像，用于沉淀公司层面的公开信息和面试风格。

建议字段：

```text
company_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

company_name
aliases
industries
target_roles
hiring_signals
interview_style
common_questions
resource_ids
question_ids
summary
```

字段说明：

- `company_id`：`company_` 前缀。
- `aliases`：公司别名。
- `industries`：行业标签。
- `hiring_signals`：招聘偏好或能力信号。
- `interview_style`：面试风格，例如项目深挖、八股、算法、系统设计。
- `common_questions`：常见题摘要。
- `resource_ids` / `question_ids`：关联资料和题目，不做存在性校验。

### 5.5 SkillRequirement

能力要求记录，用于把资料、JD、面经里的能力要求拆成可复用能力项。

建议字段：

```text
skill_requirement_id
status
source_session_id
source_artifact_id
evidence_refs
created_at
updated_at

skill_name
category
level
description
assessment_points
role_tags
company_ids
resource_ids
question_ids
```

字段说明：

- `skill_requirement_id`：`skill_req_` 前缀。
- `category`：`language | framework | database | system_design | ai | rag | agent | devops | testing | soft_skill | other`。
- `level`：`awareness | basic | working | strong | expert`。
- `description`：能力要求说明。
- `assessment_points`：面试中如何考察。
- `role_tags`：适用岗位方向。
- `company_ids` / `resource_ids` / `question_ids`：关联记录，不做存在性校验。

## 6. 来源与证据规则

### 6.1 artifact-first

任何进入 KnowledgeService 的长文本都先变成 `SessionArtifact`：

```text
后台导入面经正文 -> SessionArtifact -> ExternalResource / ExperiencePost / InterviewQuestion
知识库清洗资料文件 -> SessionArtifact -> ExternalResource / 抽取记录
题库批量导入文本 -> SessionArtifact -> InterviewQuestion
```

外部 URL 的处理规则：

- 只有 URL：保存为 `ExternalResource.url`，不抓取正文。
- URL + 正文：正文先保存 artifact，URL 作为元数据。
- 未来链接抓取：抓取结果先保存 artifact，再抽取 Knowledge 记录。

### 6.2 evidence_refs

M9 继续使用轻量字符串引用，避免第一阶段引入 store 之间的耦合。

允许前缀建议：

```text
artifact_
resource_
experience_
question_
company_
skill_req_
career_app_
resume_profile_
career_profile_
jd_
fit_
note_
sess_
```

规则：

- 只校验格式，不校验引用对象是否真实存在。
- 不允许空字符串。
- 不允许任意文件路径。
- 工具和 API 只接受受控 id，不接受 workspace path。

### 6.3 时间戳

时间戳由 store 统一维护。

规则：

- 新建时由 store 设置 `created_at` 和 `updated_at`。
- 更新和归档时只更新 `updated_at`。
- 使用 Asia/Shanghai 时区。
- 模型层不信任外部传入时间。

## 7. 存储设计

建议目录：

```text
data/knowledge/
  external_resources/
    resource_xxx.json
  experience_posts/
    experience_xxx.json
  interview_questions/
    question_xxx.json
  company_profiles/
    company_xxx.json
  skill_requirements/
    skill_req_xxx.json
```

存储规则：

- 每条记录一个 JSON 文件。
- 原子写入。
- 严格反序列化，坏 schema 直接失败。
- 损坏 JSON 抛稳定的 storage error。
- list 默认只返回 `active`，可选包含 `archived`。
- 不写 Markdown 快照。
- 不创建向量索引。

## 8. API 契约草案

第一阶段可以先不实现 API，但契约要保持稳定。

建议接口：

```text
GET    /api/knowledge/resources
POST   /api/knowledge/resources
GET    /api/knowledge/resources/{resource_id}
PATCH  /api/knowledge/resources/{resource_id}
POST   /api/knowledge/resources/{resource_id}/archive

GET    /api/knowledge/experiences
POST   /api/knowledge/experiences
GET    /api/knowledge/experiences/{experience_id}
PATCH  /api/knowledge/experiences/{experience_id}
POST   /api/knowledge/experiences/{experience_id}/archive

GET    /api/knowledge/questions
POST   /api/knowledge/questions
GET    /api/knowledge/questions/{question_id}
PATCH  /api/knowledge/questions/{question_id}
POST   /api/knowledge/questions/{question_id}/archive

GET    /api/knowledge/companies
GET    /api/knowledge/skill-requirements
```

M9 第一批实现 store 后，API 可以按资源、面经、题目优先接入；公司画像和能力要求可以第二批接入。

## 9. 工具契约

M9-3 暂停，不做聊天 Agent 写入工具。

原因：

- 外部面经、面试题、公司资料更适合后续通过知识库导入、RAG 或 MCP 读取。
- 用户自己的面经、复盘、被问到的问题和回答感受属于个人资产，应该写入 `NoteService`。
- 现在给聊天 Agent 开 Knowledge 写工具，容易把公共资料库和个人笔记混在一起。
- KnowledgeService 已经有 store 和 API，可以作为后续后台导入、清洗和检索底座。

暂不实现：

```text
knowledge_resource_create
knowledge_resource_update
knowledge_resource_archive

knowledge_experience_create
knowledge_experience_update
knowledge_experience_archive

knowledge_question_create
knowledge_question_update
knowledge_question_archive
knowledge_company_upsert
knowledge_skill_requirement_upsert
knowledge_extract_from_artifact
```

后续如果需要聊天 Agent 使用 Knowledge，优先考虑只读或检索工具：

```text
knowledge_search
knowledge_resource_get
knowledge_question_get
knowledge_company_get
knowledge_skill_requirement_get
```

只读工具的目标是让 Agent 在面试准备或学习计划中读取公共知识库，而不是让聊天过程直接写公共资料库。

## 10. Agent 契约

### 10.1 聊天 Agent 暂不写 KnowledgeService

M9 当前规则：

- 用户粘贴自己的面试经历、被问到的问题、回答、感受、反馈，进入 `NoteService`。
- 用户要求保存个人复盘，进入 `NoteService`。
- 用户提供 JD 做岗位分析，进入 `CareerService`。
- 用户要求保存长期目标或稳定偏好，按现有 memory 规则处理。
- 聊天 Agent 不把用户粘贴内容直接写入 KnowledgeService。

### 10.2 什么时候写 NoteService

应该写 NoteService 的场景：

- 用户记录自己的面试复盘。
- 用户记录自己被问到的面试题和回答情况。
- 用户记录自己的学习理解。
- 用户整理自己的回答草稿。
- 用户要求把聊天内容保存为笔记。
- 用户粘贴“这是我今天面试遇到的问题”，默认作为个人面试复盘或题目笔记保存。

### 10.3 什么时候写 CareerService

应该写 CareerService 的场景：

- 用户上传简历并诊断。
- 用户提供 JD 做岗位分析。
- 用户生成匹配报告。
- 用户生成定制简历。
- 用户维护求职项目状态。

### 10.4 什么时候写 Memory

仍按现有 memory 规则：

- 用户明确要求“记住”。
- 用户表达长期偏好、稳定目标或长期事实。
- flush / memory 管线可以保留自己的运行时摘要机制，但这不是 KnowledgeService 的职责。

## 11. 与 LearningService 的衔接

M9 不生成学习计划，但要为 M10 留好入口。

后续 LearningService 可以消费：

- `SkillRequirement`：目标岗位需要什么能力。
- `InterviewQuestion`：需要练哪些题。
- `ExperiencePost`：目标公司面试风格和常见追问。
- `ExternalResource`：推荐学习资料。
- `CareerApplication`：当前目标岗位项目。
- `Note`：用户自己的学习记录和复盘。

M9 不需要知道学习计划如何排期，只需要让资料、题目和能力要求可追溯。

## 12. 开发批次

### M9-1：领域模型和 store

状态：已完成。

文件：

```text
app/knowledge/models.py
app/knowledge/store.py
tests/test_knowledge_store.py
```

验收：

- 五类记录模型。
- ID 前缀校验。
- status 校验。
- 来源字段校验。
- evidence_refs 格式校验。
- store 统一维护 Asia/Shanghai 时间戳。
- 原子写入 JSON。
- get / list / archive。
- create / update。
- 损坏 JSON 稳定失败。
- 反序列化严格失败，不做宽松类型转换。

已完成内容：

- 五类记录模型：ExternalResource、ExperiencePost、InterviewQuestion、CompanyProfile、SkillRequirement。
- `KnowledgeStore` 文件型 JSON 存储。
- Asia/Shanghai 时间戳由 store 统一维护。
- `evidence_refs` 和关联 id 只做格式校验，不做跨 store 存在性校验。
- 原子写入、读取、列表、归档、更新。
- 损坏 JSON 和坏 schema 稳定抛 `StorageError`。
- 单元测试覆盖 M9-1 主要边界。

### M9-2：API

状态：已完成。

文件：

```text
app/schemas/knowledge.py
app/api/knowledge.py
tests/test_knowledge_api.py
```

验收：

- 资源、面经、题目 API 可用。
- 公司画像和能力要求至少可读或可 upsert。
- API 不暴露路径。
- API 不做跨 store 强存在性校验。

已完成内容：

- 五类记录均已接入 API：资源、面经、题目、公司画像、能力要求。
- 支持 create / get / list / update / archive。
- 列表默认隐藏 archived，单条读取可通过 `include_archived=true` 查看 archived 记录。
- 资源列表支持按 `related_application_id` 过滤。
- 面经和题目列表支持按 `source_resource_id` 过滤。
- API 响应只暴露受控 id 和结构化字段，不暴露内部文件路径。
- API 测试覆盖创建、读取、列表、过滤、更新、归档、缺失记录和非法更新。

### M9-3：聊天 Agent 写工具

状态：暂停。

原计划文件：

```text
app/tools/builtin_tools/knowledge.py
app/config/agent_capabilities.yaml
app/agents/default/AGENT.md
tests/test_knowledge_tools.py
tests/test_knowledge_agent_flow.py
```

暂停原因：

- 外部面经和题库后续从知识库、RAG 或 MCP 读取，不急着让聊天 Agent 写入。
- 用户个人面经、复盘和被问到的问题应该走 NoteService。
- 现在做写工具会放大边界混乱风险。

后续如果恢复 M9-3，优先做只读检索工具，而不是写入工具。

### M9-4：低成本真实链路验证

状态：调整为后续知识库导入或只读检索验证。

- 后台导入一份外部面经，生成 ExternalResource + ExperiencePost + InterviewQuestion。
- 通过 API 或后续只读工具读取资料。
- 用户个人面试复盘通过 NoteService 保存。
- 与求职项目关联时只保存 id 引用，不校验存在性。
- smoke 批次保持低，避免 token 消耗失控。

## 13. 风险与控制

### 13.1 版权和内容来源

风险：外部面经、课程、题库可能涉及版权。

控制：

- M9 不做自动抓取。
- 只保存导入、授权或后台清洗后的内容、摘要和 URL 元数据。
- 不做批量再分发。
- 未来做抓取前必须增加来源策略和内容使用边界。

### 13.2 信息过期

风险：公司岗位要求和面经会过期。

控制：

- 每条记录保留 `created_at`、`updated_at` 和来源。
- 后续可以增加 freshness 或 verified_at。
- M9 不假设资料永久准确。

### 13.3 产品边界膨胀

风险：资料库容易滑向 RAG、学习计划、爬虫、搜索引擎。

控制：

- M9 只做产品资产事实源。
- M10 再做学习计划。
- M11 再做 RAG / MCP。
- 每批开发只做一层，不跨层补功能。

## 14. 当前建议

M9-1 和 M9-2 已完成，M9-3 聊天 Agent 写工具暂停。

当前规则：

```text
外部资料 / 公共面经 / 公共题库
  -> KnowledgeService
  -> 后续通过导入、RAG、MCP 或后台管理进入

用户个人面试经历 / 复盘 / 被问到的问题 / 回答感受
  -> NoteService
```

下一步建议：

```text
1. 不做 Knowledge 写工具。
2. 用户个人面经、面试复盘和回答记录继续走 NoteService。
3. KnowledgeService 保留为外部资料库、题库、公司画像和能力要求底座。
4. 如果继续主线，可以进入 M10 LearningService，或先补一个只读 Knowledge 检索方案。
```
