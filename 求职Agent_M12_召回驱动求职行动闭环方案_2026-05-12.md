# 求职 Agent M12 召回驱动求职行动闭环方案

## 1. 一句话目标

把 M11 的 `RetrievalService` 从“能召回资料”推进到“能支撑真实求职动作”。

用户不需要记住产品记录 id，只需要用自然语言说：

```text
帮我准备星河智能二面
根据之前的短板给我今天学习任务
把这次准备内容保存为笔记
投递前帮我检查一下
```

main-agent 应该先自动召回相关求职项目、匹配报告、笔记、学习任务、短板和资料题库内容，再决定是只回答，还是写入 Note / Learning / Career。

## 2. M12 不做什么

M12 不扩展底层技术面：

- 不做索引 projection。
- 不做 embedding。
- 不做 MCP。
- 不做前端大改。
- 不做 memory 自动写入。
- 不做聊天历史全局检索。
- 不让 child-agent 直接全局召回。

M12 的重点是把已有服务串成用户动作链路。

## 3. 核心用户路径

### 3.1 面试准备

用户表达：

```text
帮我准备之前那个星河智能二面
```

流程：

```text
retrieval_search
  -> 找到相关 CareerApplication
retrieval_context_pack
  -> 召回 CareerApplication / JobFitReport / Note / LearningTask / WeaknessTracker / Knowledge
assistant answer
  -> 输出准备重点、优先级、练习问题和风险
```

默认不写入任何产品记录。只有用户要求保存、生成报告或加入计划时，才写 Note / Learning / Career。

### 3.2 学习安排

用户表达：

```text
根据之前短板给我今天学习任务
```

流程：

```text
retrieval_context_pack
  -> 召回目标岗位、匹配短板、学习计划、未完成任务、资料题库
learning_task_create / learning_task_update_state
  -> 只有用户明确要求创建、加入计划或监督时才写入
assistant answer
  -> 告诉用户今天做什么、为什么做、完成标准是什么
```

LearningTask 只保存短任务、状态、引用和完成标准，不复制长篇资料正文。

### 3.3 保存准备内容为笔记

用户表达：

```text
把这次准备内容保存为笔记
```

流程：

```text
retrieval_context_pack
  -> 找到本次准备依据
note_create / note_append
  -> 保存用户可见、可编辑笔记
assistant answer
  -> 告知已保存
```

Note 的 `evidence_refs` 和 `source_refs` 必须来自真实召回结果或当前会话工具结果，并使用 NoteService 当前支持的受控引用类型。Learning / Knowledge 来源可以在正文中说明，或通过其关联的 application、fit、note、artifact 追溯。不要写 memory。

### 3.4 投递前检查

用户表达：

```text
投递前帮我检查一下
```

流程：

```text
retrieval_search
  -> 定位 CareerApplication
retrieval_context_pack
  -> 召回 ResumeProfile / JDAnalysis / JobFitReport / CareerApplication
career_application_merge
  -> 只有需要沉淀项目状态、风险和下一步行动时才更新
assistant answer
  -> 输出风险、行动项和是否建议投递
```

不要重新委派 `resume_agent` 或 `job_agent`，不要重复解析简历或 JD。

## 4. 写入边界

```text
只读回答：
  面试准备建议 / 复盘解释 / 今日建议

写 Note：
  用户明确要求保存为笔记、复盘、答案草稿、面试题记录

写 Learning：
  用户明确要求创建任务、加入计划、监督执行、记录进度

写 Career：
  用户明确要求推进求职项目，或投递前检查需要更新 summary / risks / next_actions

写 Memory：
  仍然只按原 memory 规则处理长期偏好、稳定目标或长期事实
```

## 5. Agent 契约

main-agent 必须遵守：

- 没有 id 但用户提到历史求职上下文时，先召回。
- 召回多个候选项目且无法判断时，先让用户确认。
- 默认不把面试准备或学习建议自动写入产品记录。
- 写入产品记录时，`evidence_refs` 必须来自本次召回或当前会话真实工具结果，并符合目标服务当前支持的引用格式。
- 不把 workspace path、未知 id 或自然语言结论当证据。
- child-agent 不直接全局召回；需要资料时由 main-agent 传入明确 id。

## 6. 第一批开发范围

状态：已完成。

已完成文件：

```text
app/agents/default/AGENT.md
tests/test_retrieval_action_flow.py
docs/product_service_roadmap.md
求职Agent_M12_召回驱动求职行动闭环方案_2026-05-12.md
```

验证目标：

- 面试准备：召回后只回答，不写入。
- 学习安排：召回后按明确要求创建 LearningTask，不写 memory。
- 保存笔记：召回后写 Note，不写 memory。
- 投递前检查：召回后更新 CareerApplication，不重新分析简历或 JD。

## 7. 当前结论

M12 不是新底层服务，而是把 M7 到 M11 的产品资产层、召回层和 agent 契约接成真实用户动作。

如果这一步验证稳定，下一阶段应该优先打磨用户体验和产品主流程，而不是继续加 RAG 技术复杂度。
